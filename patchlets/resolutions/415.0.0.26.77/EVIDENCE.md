# Resolution evidence: Threads 415.0.0.26.77

## 2026-09-04 patchlet 090 r53 shared proof-tool maintenance

Revision 53 updates only shared release-proof metadata and tools: the resolution schema owns the exact eleven-string `AutoBlockSync` signed-wrapper targeted-JADX baseline ending in `currentPassiveMatch(`, and the release contract AST-proves the wrapper is identical while rejecting stale, missing, duplicate, and nonliteral fixtures. Runtime Java/Smali and APK behavior are unchanged. This historical 415 resolution does not supply current Threads 444 release authority, and no 415 APK was built, signed, published, installed, or runtime-tested by r53.

## 2026-09-04 patchlet 090 r52 shared proof-tool maintenance

Revision 52 updates shared release-proof expectations without changing this historical 415 runtime, bridge rendering, published APKs, or release authority. The current combined bridge contract is 325 cases: 279 legacy regression fixtures plus 46 current `single-target` fixtures, comprising one positive and 45 negatives. Its ten new invalid-generation negatives cover four consumer call/accessor clobbers, four producer register-clobber/zero-route mutations across `BlocklistStore.lookupId` and `isCurrentIdMatch` normal/typed-handler/catch-all flow, and two shared-callee mutations proving unchanged `IdMatch.access$200` `String`/wide forwarding plus `IdMatch.invalid(String,J)` construction with `max(0, observedGeneration)`.

The three exact recovered `AutoBlockSync` latch forms remain selected-version targeted-JADX evidence only. JADX local names and a rendered catch generation of `0L` are non-authoritative; raw signed DEX must establish the producer-to-consumer register and handler flow. Historical 415/r47-r51 reports below retain their frozen 315-case and earlier counts. They are not rewritten or promoted by this shared-tooling maintenance, and this resolution does not supply current Threads 444 release authority.

## 2026-09-03 patchlet 090 r50 fixture and historical boundary

Revision 50 preserves the ordinary 415 proof evaluator while repairing only the shared collision fixture's null handling. A controlled probe showed that adding the two missing split-tool pins makes this historical resolution pass the current schema, but the current catalog still cannot pass: after two stale historical test-harness pins, it reaches the absent current `delivery` section. Adding that section would broaden historical runtime authority, so the conditional split-pin addition was not retained.

The shared final r50 release-contract report `work/r50-release-contract-final-b.json`, SHA-256 `6a013fe93b5f3b5e3ca80a04d8daf93e4a4776fd901dfa82dc5170f0d75773bd`, passes ordinary-path compatibility alongside the strict 444 collision fixtures and records release-gate revision 50. It is not a fresh 415 catalog, release replay, or runtime result.

## 2026-09-03 patchlet 090 r49 proof-engine compatibility

Revision 49 adds a descriptor-bound two-candidate mode to the shared semantic-proof schema and evaluator for the Threads 444 `LX/02ja;` / `LX/02jA;` Windows filename collision. The historical 415 null-seed proof remains an ordinary exact-path proof at `smali_classes4/X/3Oy.1.smali`; focused release-contract evidence exercises that ordinary mode alongside both 444 collision arrangements. This shared proof-engine change does not alter the 415 bridge, resolution semantics, published artifact, or runtime boundary.

The focused r49 release-contract report `work/r49-release-contract-final.json`, SHA-256 `0735adbcfe276da017949b861b25f917581f0444655ac980f50a649cb1aaeb7d`, passes the ordinary-path compatibility case together with the strict 444 collision positives and negatives. The current full resolution schema includes later 444-only release requirements, so this compatibility result is deliberately not presented as a fresh full 415 catalog or release replay.

## 2026-09-03 patchlet 090 r48 shared bridge-evidence pins

Revision 48 retains the historical 415 bridge implementation and fixture behavior while making its shared evidence explicit. The ordinary Block and callback-free passive-preflight paths account for two reviewed `prepareModel` calls and two calls through each cache lookup/factory/placeholder seam; the resolution now pins those cache counts to `2` beside the existing prepare/preflight counts. The signed wrapper, current generated carrier, and legacy regression carrier must agree on the same raw, definition, caller-provenance, and passive fail-closed evidence through the hash-pinned AST contract. This rebinding changes no 415 runtime code and does not replace the historical release boundary.

This resolution is the reviewed symbol map for source APK SHA-256
`0f6b4515902f78ea194b1bc0563bfcab07f2559d8e80cf6f329d3d1ef786487e`.
It is not valid for a different APK, even if that APK has a similar filename.

## Version-sensitive seams

- `BarcelonaActivity.onResume()` holds the non-null `UserSession` in `v6` at
  the selected post-`invoke-super` hook.
- `X/023.A0f` performs cached numeric-ID model lookup and returns `X/2fp`.
- `X/2gx.A00` returns the session-owned `X/2gy` user cache.
- `X/2gy.A02(X/2fh, String)` is the native cache `getOrPut` seam and returns
  `X/2fp`. Its exact `X/3Hp` default factory turns a null seed plus the requested
  String into an initialized placeholder model, while `X/3Oy.1` proves a native
  `UserId` wrapper calling the same seam with a null seed and its stored String.
- `X/2gb.A0M` reports whether the resolved model is already blocked.
- `X/DNo.A00`, operation `0`, builds `friendships/block/%s/` and calls `X/Mwt`.
- Callback terminal mappings are encoded in the bridge templates and must be
  re-proven when any interface method set changes.

### Left drawer Settings row

- `smali_classes3/X/0MO.smali` owns the feed-menu item sequence, and the exact
  `feed-menu-bottom-spacer` keyed item is the true-bottom insertion boundary.
- Rule `drawer-settings-button-hook` inserts one keyed
  `threadsmod-drawer-settings` item immediately before that spacer. It does not
  add a launcher, exported activity, or intent filter.
- `X/0M9.A01` is the native drawer row renderer, and `X/9ch.A0N` constructs the
  existing 64 dp drawer-row modifier. Their exact descriptors are resolution
  bindings rendered into the three patchlet-050 drawer templates.
- The click interface and Kotlin unit singleton are also exact bindings. A
  missing/ambiguous key, row signature, modifier signature, or anchor invalidates
  this resolution rather than permitting an approximate menu injection.

### Inline action-row topology

- `smali_classes2/X/0sC.smali` contains the shared dense post/reply action row
  and the native Share helper tagged `feed_post_ufi_share_button`.
- The Share helper's Compose object is live immediately after the call, but the
  later popup path overwrites its original register. Rule
  `inline-action-spacing-capture` preserves that object in the reviewed spare
  register before the overwrite.
- Rule `inline-action-row-hook` inserts exactly one Block adapter only after the
  two exact `X/8qi.A0H` container-close calls and before the next optional
  action. The host anchor contains no Report renderer. The Block adapter calls
  the patchlet-070 request factory internally and passes its returned bounded
  `ReportRequest` beside the same immutable Block request to the sole row click.
  Both anchors, the one-render count and the register plan are mandatory proof
  for this digest.
- Rule `inline-author-snapshot-host-method`, owned by patchlet 060 r9, appends
  one private helper to the exact-SHA `X/0sC` host class. That helper alone
  reads the row media key, resolves `X/6wn` and its displayed `X/2fp` author,
  calls the reviewed author `getId()` and `X/021.A1B` username seams, and creates
  one immutable `InlineBlockRequest` carrying bounded identity plus the author
  and media models only as opaque `Object` values. The action-row hook passes
  that same request to `InlineActionRowAdapter`; the adapter passes it unchanged
  to patchlet 070 r7's `InlineReportActionFactory`, which returns data rather
  than a click action.
- Stable inline and Report classes/templates no longer resolve media, author,
  author ID, or username themselves and contain no raw `getId()` or
  already-blocked private seam. Patchlet 090 r31 scans the signed primary DEX
  across both mod descriptor prefixes for those calls rather than trusting
  source text or targeted JADX alone. Patchlet 070 casts only the request's
  opaque captured media model for
  the exact `X/6wn.A04` -> `X/7A4.B2O` -> `X/6yd.CRt` caption path. Username,
  caption, immutable row identity, one-host-helper ownership, and the absence of
  duplicate stable private calls are mandatory proofs for this source digest.
  Patchlet 070 r7 additionally reads the resolution-owned `Bwc()` permalink
  accessor from that same captured media receiver and passes its live value to
  the sixth String constructor slot; it performs no second lookup.
- Patchlet 070 renders no Report UFI button, icon or test tag. Exactly one UFI
  helper call and one `threadsmod_inline_block` tag exist across both template
  families. The exact author, caption, register and row-identity proofs are
  mandatory for this source digest.

## Stable seams

- The injected runtime takes the Threads session only at the native bridge.
- Clone Blocker list GETs never receive that session or viewer ID.
- List envelopes remain mandatory Ed25519 documents under the pinned key.
- The ISP-blocked origin is forbidden in packaged DEX strings.
- The single inline Block control always opens one compact **Block and report**
  modal. It shows bounded identity, the selected report reason, immutable
  Unicode-safe post excerpt, persisted **Also block this profile** checkbox,
  concise disclosure, one dynamic positive action, and Cancel. The positive
  action is labeled **Block** when checked and **Report** when unchecked; either
  state creates one distinct durable report enqueue, while checked additionally
  schedules the same immutable numeric target through the shared Block
  scheduler. `ui_also_block_profile` is synchronously committed and restored
  after process restart. There is no dedicated Report action, `ReportDialog`,
  `InlineReportClick`, editor, `Review report`, preview, consent checkbox, note,
  second modal, or one-click bypass. The removed `Safe processing` and `Before
  exact review` copy plus the old Report tag remain final-DEX forbiddens. A
  report-capable entry remains present for an already blocked target and after
  callback-confirmed Block success.
- The obsolete injected `UserFetchCallback` class and its `lookup_failure` and
  `lookup_busy` stage literals are forbidden in final DEX. The replacement
  direct-ID bridge separates session cast, cache lookup, cache factory, and
  placeholder construction into narrow regions; the resolved-row entry
  separately isolates session and opaque-model casts. Both dispatch to
  `blockModel` outside try. Callback-free `prepareModel` isolates model-ID access
  and the already-blocked predicate. `blockModel` interprets its fixed result
  outside try and separately protects only the preparation invocation/result and
  native mutation invoke. Every mod callback remains outside protected ranges.
  `ThreadsBlockBridge` routes ten fixed failure results and one fixed success
  result through stable `BridgeCallbackDispatcher`; `MutationCallback` routes
  native started, failure, and success there too. Its checked `Handler.post` to
  `Delivery.run` hop is the only owner of real mod-callback invocations. It
  places downstream mod code beyond both the scheduler bridge-call catch and
  synchronous host/private stack, so a mod exception cannot be caught and
  mislabeled as `bridge_exception` or `mutation_exception`.
  Automatic/manual literal-true scheduler ownership handoff and false-return
  occur before the sole bridge-selection/argument-load/invocation-only
  `Throwable` caller catch; its outside
  handler routes fixed `bridge_exception` to the exact failure owner. Their six
  `BridgeCallback` methods consume `Delivery.run` directly with the reviewed
  started/status, failure, completion-save, quarantine-before-release, success,
  ownership-release, and continuation effects. Accepted automatic-current and
  manual-completion-CAS branches cannot return, catch-and-return, or loop before
  their exact save. Every asserted argument has a live CFG-dominating reaching
  definition and no scalar/wide clobber before its call; dead lexical fields,
  constants, tokens, Handlers, delays, and Runnables do not count. Every
  covering handler is an exceptional CFG successor and
  normal handler-tail entry is forbidden. After saved automatic success clears
  `waiting`, only reviewed finish, foreground-change finish, or exact-`MAIN`
  paced-post terminals remain normally or exceptionally reachable. Automatic
  normal success owns one nonzero pace-derived next-only
  delayed Runnable. Immediate relays, explicit-throw exits, unreachable
  terminal decoys, and no-op success bodies fail closed, so confirmed success
  cannot later become timeout/retry state.
  `BlockDiagnostic` reports bounded
  identifier-free codes `CB-BRG-106` through `CB-BRG-113` with `mirror=n/a` and
  `bridge=r5`, including `model_id_exception` / `CB-BRG-112` and
  `already_blocked_exception` / `CB-BRG-113`, without copying exception text,
  target IDs, session data, or URLs into local status/history strings.
- Patchlet 090 r31 retains the resolution-owned signed-primary-DEX bridge-flow
  contract established by r24. The verifier must prove the raw private call descriptors; immutable
  target/model-ID register flow; this cast/cache/preparation/result/mutation
  topology; exact entry-connected `block`, `blockResolved`, `prepareModel`, and
  `blockModel` paths with leading-return/unreachable-decoy shapes rejected;
  fixed stage and callback arguments; exact automatic-run,
  manual-run, and bridge-owner caller provenance with zero other callers;
  exact reviewed ownership of every resolution-supplied raw private seam call
  with no extra mod-namespace caller; the exact native callback interface;
  automatic/manual handoff, catch, fixed-handler, and real callback-effect
  topology; direct execution of all six scheduler callback consumers with no
  immediate repost and a sole reviewed paced continuation;
  exact `AutoBlockSync$FetchWorker;->run()V` and
  `AutoBlockSync;->scheduleManualDrain(J)V` definitions alongside the two
  caller classes; live Handler/Runnable/delay provenance and checked boolean
  acceptance at all six scheduler-critical enqueues; watchdog expiry/refusal
  reaching automatic no-retry quarantine or inline abandoned review;
  every bridge/native dispatcher route and its caller provenance; a checked
  `Handler.post`; zero real callbacks outside `Delivery.run`; and the native
  mutation identity. Its hash-pinned contract matrix contains one positive and
  271 exact negative fixtures. The frozen inspector remains unchanged at
  SHA-256
  `937269c9d989f36148f6d24bb68343d3963a419f57a5d78a4d9df82c08771cd2`.
  Before that harness can count as signed-APK evidence, the release contract
  parses both PowerShell scripts and proves one ampersand invocation with the
  exact ordered named parameters `ScratchRoot`, `ResolutionPath`, `Java`,
  `Javac`, and `ReportPath`; splatting, duplicate names, positional extras, and
  any name absent from the callee's parameter block fail closed. R25 also gates
  the one-modal topology, sole dynamic positive action, report-always and
  Block-optional routes, persistent checkbox ownership, initiating
  Activity/viewer binding, distinct per-tap durable report insertion, SQLite v2
  migration, and final-DEX absence of the retired report classes and UI paths.
  Targeted JADX is only complementary readable evidence and its required
  strings do not pin a decompiler-generated private-class alias.
- Reporting authority exists only at the combined modal's positive-action tap.
  It accepts a non-empty normalized username, canonical numeric profile ID,
  Unicode-safe caption excerpt capped at 280 UTF-16 units, selected reason, and
  the immutable captured row's post permalink. Every new queue requires HTTPS,
  exact current `www.threads.com` or legacy-input `www.threads.net`, no
  user-info/port/query/fragment/encoding, an exact username-bound post path,
  and canonical output under `https://www.threads.com`; the payload uses that
  value as `targetUrl` and the sole entry of `evidence`. Legacy stored rows with
  an empty link remain decodable but cannot authorize new work. The queue call is bound to the
  initiating foreground Activity, live viewer, and unchanged owner generation;
  it derives the pseudonym/language/time-zone payload off-main and commits one
  fresh report ID to the viewer-scoped `threadsmod_reporting.db` outbox before
  starting foreground-only delivery to the exact reviewed AWS relay. The modal
  concisely discloses the pseudonym, language/time zone, server-visible IP and
  User-Agent, possible coarse location, and operator access; full retention and
  local outbox/history detail remains in the private Settings/Activity UI.
- The outbox, attempt reservation, retry, cancellation, terminal transition,
  integrity-review state, 1,000-row pending bound, 500-row history bound, and
  16 KiB UTF-8 payload bound are patchlet-070-owned. ReportStore v2 removes the
  former `UNIQUE(viewer_key,target_key)` constraint so every explicit row tap
  inserts a fresh primary-key report ID even when another pending report targets
  the same profile. The only accepted v1-to-v2 migration validates the exact v1
  tables, indexes, history shape, and `quick_check`, copies every outbox row and
  verifies its count, swaps the table, and recreates the due index in one
  transaction; any version, shape, integrity, copy/count, or DDL mismatch rolls
  back without resetting data. A full foreground-generation preflight rejects
  self-target/cross-table lifecycle corruption before POST; indexed row
  mutations, insertion-only history trimming, plausible timestamps, and
  overflow-safe wakes preserve bounded operation. A wrong-type or corrupt
  committed pseudonym secret is surfaced as exact review state immediately
  after pseudonym lookup even when both tables are empty, and next-wake schedules
  no delivery. Relay acknowledgement is accepted only as complete valid-UTF-8
  strict duplicate-free JSON with one boolean `ok:true`. A 15-attempt `gave_up`
  row means acknowledgement remained unconfirmed and never claims the server did
  not accept a POST; another report requires another explicit modal tap. No
  automatic report-creation path exists. The checked checkbox may additionally
  schedule Block, but Block callbacks never create a report.
- The UI/report active viewer exists only in process memory after the live
  session hook validates it. The old `ui_active_viewer` preference is removed
  but never read or written, so an Activity restored after process death has no
  report scope until the signed-in feed is re-entered.

## Resolved inline author-model, username, and direct-ID handoff

The previously published resolved-inline candidate used an injected
`UserFetchCallback` and made Threads' authenticated `users/%s/info/` response a
prerequisite for cache-miss automatic and hintless Block work. The current
exact-SHA resolution removes ownership of that profile-info fetch, callback
interface, and both legacy lookup stage literals. Canonical list reads remain
ordered GitHub raw, jsDelivr, then the AWS blocklist relay. Report delivery
remains the single explicit-positive-action AWS `/v1/reports` write; no
alternate write or `tree55.com` fallback is authorized.

For an inline post or reply, the action-row resolution proves the displayed
`LX/6wn` media model, its `LX/2fp` author, the author's `getId()` accessor, and
the `X/021.A1B` username accessor. Patchlet 060 r9 owns those private reads in
one appended exact-SHA `X/0sC` helper, then preserves both native models only as
opaque process-local `Object` values inside one immutable request. The stable
adapter and patchlet 070 r7 Report factory consume that same request; the
factory returns one bounded same-row `ReportRequest`, and the username remains
only bounded display and scheduler metadata:

- the host helper binds the author model, media model, bounded `@username`
  label, media key, and numeric author ID once; the row adapter, Report factory,
  click, and controller do not re-resolve a host identity, and the label never
  replaces the numeric mutation identity;
- the Report factory uses the same request's captured media model only for the
  exact caption extraction path, returns data rather than a click/editor action,
  and contains no raw author-ID or already-blocked seam;
- `AutoBlockSync` commits the viewer-scoped manual queue item before staging a
  hint in a 64-entry, ten-minute, viewer/target/session-scoped registry;
- queue-start persistence and attempt reservation both complete before the
  scheduler consumes the hint;
- SHA-rendered bridge Smali casts the object, reads the exact reviewed model ID,
  and requires equality with the requested target before already-blocked or
  mutation logic; invalid or mismatched objects fail closed.

Automatic, restored, and otherwise hintless work first uses the ordinary
`X/023.A0f(session, targetId)` cache lookup. On a miss, the bridge calls the
exact `X/2gx.A00(session)` factory and then
`X/2gy.A02(null, targetId)`. The returned native model is not trusted as new
identity: the shared `blockModel` path re-reads `getId()` and requires exact
equality with the independently validated target before any mutation. A null
placeholder fails as `placeholder_model_invalid`; a mismatched model fails as
`model_id_mismatch`. Private-seam exceptions are classified at their exact
boundary. Direct-ID session/cache/factory/placeholder operations use
`session_model_exception`, `cache_lookup_exception`,
`cache_factory_exception`, and `cache_placeholder_exception`; resolved-row
session and model casts are isolated before dispatch outside those try ranges. Callback-free
`prepareModel` returns `model_id_exception` or `already_blocked_exception` when
those exact private calls throw, `model_id_mismatch` for a null/unequal ID,
fixed internal sentinel `already_blocked_success`, or null for mutation-ready. `blockModel`
maps an unexpected preparation-invoke failure to `bridge_dispatch_exception`,
interprets every result outside try, and uses `mutation_exception` only around
the raw mutation invoke. None copies the caught object or message into
persistent diagnostics, and no mod callback is inside a private-seam range.
All ten fixed bridge failures, the bridge's one fixed success, and native
started/failure/success routes cross `BridgeCallbackDispatcher`'s checked
`Handler.post` boundary before `Delivery.run` invokes the real mod callback.
There are no real callback invokes elsewhere. That prevents the scheduler
bridge-call catch or synchronous host callback delivery from turning downstream
mod exceptions into bridge- or mutation-seam failures. No
profile-info network fetch is a bridge prerequisite.

The exact host helper calls `DFF` synchronously only after its accepted-user
type guard. An invalid type can return before any native callback. The bridge
does not treat that silence as success: the automatic and manual scheduler
watchdogs close it as review-required `callback_timeout` uncertainty, quarantine
the target, and authorize no automatic retry. Static evidence
does not prove a live account will always pass that host guard; this remains a
runtime boundary.

The bounded `X/2gy.A02` proof also pins its `X/3Hp` factory. On the default path,
that factory converts a null `X/2fh` seed plus the requested String into a new
`X/2fh`, wraps it in `X/2fp`, retrieves the `X/2gy.A00` session, and calls
`X/2fp.A06` for session initialization. Separately, `X/3Oy.1` identifies an
`X/1R1` value (whose exact class labels itself `UserId(id=...)`), extracts its
String, passes a null seed, and invokes the same `X/2gy.A02` method. These facts
bound the direct-ID model construction role to this source digest; they do not
make it a stable API.

These exact symbols, method descriptors, null-seed call site, username accessor,
final-Dex legacy-string absences, and targeted recovery contracts are static
evidence only. The cache-created placeholder's live acceptance by Threads and
a real foreground account mutation remain explicit device/account runtime
boundaries. This revision has not yet received a pristine release replay and
must not inherit the older APK's runtime or publication claims.

## Current-source combined action, bridge, diagnostic, and completion-review remediation

Patchlets 020 r13, 030 r5, 050 r9, 060 r9, 070 r7, and 090 r31 extend the
reviewed direct-ID source contract without reviving the retired profile-info lookup.
The exact-SHA `X/0sC` helper creates one immutable row request containing the
same media key, numeric profile ID, bounded `@username`, opaque author model,
and opaque media model. The Block adapter and Report factory share that request;
neither stable template resolves the private media/author/ID path or invokes the
already-blocked seam. The label remains local presentation/queue/history
metadata and is never mutation identity. Every tap opens the sole **Block and
report** modal with reason, immutable excerpt, persisted **Also block this
profile**, concise disclosure, one dynamic positive action, and Cancel. The
positive tap always requests one fresh durable report enqueue; checked also
schedules the same immutable numeric target through the shared Block scheduler.
Dedicated Report/editor/review/consent/one-click routes are absent.

Terminal Block-failure ownership is explicit and non-overlapping. Every
optional-Block branch that ends before durable Block enqueue records one closed
diagnostic before invoking its UI failure callback. Once that Block item is
durably queued, only the shared scheduler may record its terminal state; report
queue/storage outcomes remain patchlet-070-owned. An automatic attempt-reservation
failure records history, one viewer-scoped alert, bounded status and bounded
log output, then returns without calling `ThreadsBlockBridge`.

Patchlet 020 r13 advances every bounded Block diagnostic to `bridge=r5` and
maps the two new private-stage failures to fixed identifier-free codes:
`model_id_exception` is `CB-BRG-112`, and `already_blocked_exception` is
`CB-BRG-113`. Patchlet 030 r5 removes the former wide catch/shared mutable stage
register and routes native terminal callbacks through the stable asynchronous
dispatcher. Patchlet 090 r31 retains signed-primary-DEX bridge-flow verification,
including the `Handler.post`/`Delivery.run` firewall and exact private-caller
ownership, as a blocking release gate and keeps targeted JADX alias-free and
secondary. Its release-contract source also uses the PowerShell AST to bind the
sole fixture invocation to the exact ordered declared-parameter set and binds
the duplicated reviewed-terminal-route expectation to the same literal five
before any signed candidate may count as release-validated evidence.

The separate `report-post-permalink-contract` is also blocking. The resolution
pins a 21-argument signed-primary-DEX inspector and a 20-case fixture matrix.
It proves same-captured-media caption/permalink receivers, the sixth String
constructor slot, the sanitizer result as the sole class-wide write to a final
permalink field, exact literal-boolean reaching definitions through the reviewed
shared validity return, all-path base/nonempty validity plus controller/client
queue guards, and the same immutable getter value in `targetUrl` and the sole
entry of one no-argument-initialized attached evidence array on the unique
populated root. Payload self-validity must route its invalid fallthrough only to
one pre-write empty-root return; the sole handler reaches one distinct catch
root. Negative cases cover clobber/wrong slot, raw/later/non-final storage,
receiver divergence, exception bypass, missing queue guards, an extra true
definition, a bypassed payload-validity branch, a substituted populated root,
different payload values, duplicate/indexed array mutation, and throwaway
roots/arrays. The signed wrapper
and positive harness are AST-bound to the same ordered twenty resolution member
references plus APK. Strict sanitizer behavior is not inferred from the flow
fixture: the host harness is authoritative behavior evidence, while the
nineteenth signed targeted-JADX class `ReportValues` pins the reviewed URI,
host, authority, path, username, and canonical-prefix implementation.

The frozen r30 replay `work/patchlet-post-permalink-r30-20260902-a` reached a
signed run-local candidate and failed closed at `release-gates` with
`request_validity_true_return_count`: the old scalar origin lattice merged the
literal-one and literal-zero assignments at D8's shared return. Its build report
records `artifactProduced: false`, a null output binding, no publication move,
verified rollback, and runtime `not-run`. Direct r31 inspection of that candidate
passes the bounded shared-return and early-root/catch topology, but those bytes
remain unpublished failure evidence and do not count as the r31 release.

The fresh exact-source r31 replay
`work/patchlet-post-permalink-r31-20260902-b` passed `build-report.json`,
`release-check.json`, and `idempotency-check.json` and transactionally published
`dist/ThreadsMod-CloneBlocker-415.0.0.26.77-arm64-v8a-post-permalink.apk`.
The published APK is 81,305,196 bytes with SHA-256
`47f8f70fdf615463de336ddb34edd34c2f81f4e4014b2fa4f8d85f19b5dc5cd3`.
The release report binds application ID `com.threadsmod.barcelona`, version
`415.0.0.26.77` / `508504469`, minimum/target SDK 28/36, one v2 Android Debug
signer certificate SHA-256
`317e3f3813f3b1ec324717faf1bb78f954f49a12122181a85b42338ca10dd079`,
and 16 KiB alignment. Complete-tree reapplication was identical across 115,685
files, 7,821 directories, and 123,506 entries with SHA-256
`46b2c64c61d976783d8759d90412dce8cf8c93200ae73bc68356a98f8fad669c`
before and after. Signed release evidence passed 275 bridge fixtures, 20 permalink
fixtures, 19 targeted-JADX recoveries, 12 root/13 total DEX payloads, 62,168
primary-DEX method references, 11 native libraries, archive integrity, and
signer continuity. Runtime/device/account/network validation remains `not-run`.

Patchlet 020 r13 also checks Android's boolean acceptance result at all six
scheduler-critical Handler enqueue sites. Fetch-to-main rejection releases
ownership; pacing and normal-success continuation rejection close the current
run; shared-drain rejection records a bounded pause while durable work remains.
Automatic and inline watchdog expiry or rejection happens after bridge dispatch
and therefore reaches review-required `CB-MUT-205`, not transient backoff. The
automatic target enters completion-review quarantine with no automatic retry;
the inline item is stored as abandoned and can run again only through explicit
atomic Retry. Optional toast delivery is the sole unchecked post.

Native success followed by failure of the ordinary local completion save is
not routed through transient backoff. The scheduler first installs a bounded
process-local viewer/target latch and then attempts the strict viewer-scoped
`completion_review_threads_<viewerId>` StringSet. Both target selection and the
final pre-dispatch check exclude matching targets. No backoff deadline,
automatic queue entry, or automatic retry is created. The persisted set is
bounded to 200 targets; corrupt, oversized, or full state durably pauses
automatic work, while read/commit exceptions or commit failure retain the
process-local pause. `CB-LOC-301` reports only `review-saved` or
`review-local-failclosed` recovery state and copies no viewer or target ID.

Only explicit Activity **Retry** or **Retry all** may release this review state.
The preference transaction moves only the selected retryable manual item(s)
and removes their matching quarantine atomically; only after that commit may
the corresponding process-local latch be cleared. Unrelated queued/running
manual work is not reset or evicted.

These statements describe current canonical source and exact-resolution gate
requirements. The revision-21 `-b` and revision-23 `-c` replays below reached
signed run-local candidates but failed before publication. Reusing either
candidate in a repaired standalone check did not turn its frozen inputs into a
later-revision replay. The successful revision-24 `-d` replay and publication
recorded below are historical predecessor evidence and do not validate current
r13/r8/r9/r6/r25 behavior. Emulator and phone/account runtime results remain
pending, and all historical evidence remains bound only to its recorded inputs
and outputs.

## Failed cache-ID diagnostic attempt

`work/patchlet-cache-id-diagnostics-20260901-a` froze source APK SHA-256
`0f6b4515902f78ea194b1bc0563bfcab07f2559d8e80cf6f329d3d1ef786487e`,
resolution-file SHA-256
`de548176b9b7c9290de8eb09014203158b94c8c535ffd48143644ec434653801`,
resolution-directory SHA-256
`3db1c563338bdadf826de8139b5e5ea0be187bb4790f5390fce205d1ffff2a72`,
and canonical patchlet-tree SHA-256
`4be43d65957b3a869a9ee755918b8d706ec33c09df3af4d48f6adbf2ec3b5846`.
Those bindings describe that failed run only and are not current canonical
hashes.

The run reached release gates, where targeted JADX recovered the former broad
catch/shared-stage bridge with `Not initialized variable reg:` and a synthetic
`strOnBridgeFailure` local. The frozen resolution also required the
decompiler-created spelling `AbstractC68772gx.A00(`, while the recovered code
used another synthesized alias. The recorded failure is therefore
`Targeted JADX recovery for 'threadsmod.autoblock.ThreadsBlockBridge' is missing
'AbstractC68772gx.A00('.`

The build report records `status: failed`, `artifactProduced: false`, a null
output binding, a null published binding, `publicationState.moveAttempted:
false`, verified publication rollback, and `runtimeValidation: not-run`. No APK
was published. This is bounded failure evidence for why private alias spelling
and the old wide-catch/uninitialized-stage shape cannot authorize release; it is
not replay, idempotency, signed-artifact, release, emulator, or account/runtime
evidence for patchlets 020 r12, 030 r5, or 090 r23.

## Failed release-tool parameter-contract attempt

`work/patchlet-cache-id-diagnostics-20260901-b` froze the same pristine source
APK SHA-256
`0f6b4515902f78ea194b1bc0563bfcab07f2559d8e80cf6f329d3d1ef786487e`,
resolution-file SHA-256
`339d4ad46d997abc4a2b871e483f60ab57101f3b6a948c089c18d14b7ef8d457`,
resolution-directory SHA-256
`a2e44a979da42decdf50707c173bf8b878c0327fb0992f4c4501caaa6e35b575`,
and canonical patchlet-tree SHA-256
`9cc628af31148b3242346df7f4a37c57794361e377242a7a743b127678bb33cf`.
Those hashes bind only the failed revision-21 run.

Its complete decoded-tree idempotency proof passed over 115,711 files, 7,821
directories, and 123,532 entries. Before and after SHA-256 both equal
`fb260d28b642bc2a52d7f9ca2c0b49e84429ecedbf1e94a8d47b327fb944bf66`.
The pipeline then rebuilt, aligned, and v2-signed a run-local 81,309,292-byte
candidate whose supplemental SHA-256 is
`d93a6c0a5486d60117675668727633dacf3fa2a6abc630c169ba717d25891448`.
That candidate is failure evidence only: `release-check.json` was never written,
the destination remained absent, and no static gate result or installability is
inferred from signing.

The build report (SHA-256
`2dcf179bd7e69b8fdc96b189d5a6a5d6ffc0eae2525318df307b48655c5c3c7f`)
records `status: failed`, `artifactProduced: false`, `failedStage:
release-gates`, null output/published bindings, no publication move, verified
rollback, and `runtimeValidation: not-run`. The exact failure was an undeclared
`AndroidSdk` argument passed by `Test-PatchedApk.ps1` to
`Test-DexBridgeFlowInspector.ps1`. This is an r21 input failure: no later source
revision may relabel its signed candidate as replay evidence. The historical
patchlet 090 r24 correction, retained and generalized by current r28, removes the unsupported argument, pins the changed
gate tools, and parses the caller and callee PowerShell ASTs. It requires one
ampersand fixture invocation with only the exact ordered `ScratchRoot`,
`ResolutionPath`, `Java`, `Javac`, and `ReportPath` names, verifies that every
name is declared by the callee, and rejects splatting, duplicates, missing
arguments, and positional extras.

After that call site was repaired, the standalone validation at
`work/release-validation-cache-id-b-r22-a` reused the same quarantined signed
r21 candidate only to discover the next static failure. It reached the frozen
signed-Dex bridge-flow inspector and failed
`private_author_id_unreviewed_caller`. Exact DEX inspection found three
mod-namespace raw author-`getId()` callers: the reviewed bridge owner plus the
then-stable patchlet-060 `InlineActionRowAdapter` and patchlet-070
`InlineReportActionFactory`; the adapter also retained a direct already-blocked
private seam that would violate the same sole-owner architecture. This
standalone failure produced no release result, publication, install, emulator,
or account/runtime evidence and does not upgrade the r21 candidate to r22.

The then-current cataloged correction was patchlet 060 r8's one exact-SHA `X/0sC` host
snapshot helper, patchlet 070 r5's consumption of the same immutable request
and captured media object, and patchlet 090 r24's AST parameter and signed-Dex
caller-ownership gates. Stable Block/Report templates now contain no raw media
lookup, author lookup, author-ID, username, or already-blocked seam. The
272-case inspector and its SHA-256
`937269c9d989f36148f6d24bb68343d3963a419f57a5d78a4d9df82c08771cd2`
were not weakened or changed to admit the old templates.

The current r28 resolution retains those alias-free targeted-JADX requirements,
forbids the old uninitialized-stage markers, and declares the signed-Dex
`threads-block-bridge-flow-v1` contract as required. The authoritative result
binds the exact automatic-run, manual-run, and bridge-owner call sites with zero
other callers; every resolution-supplied raw private seam call to its reviewed
owner; zero private media-lookup, author-lookup, and username calls across every
`Lthreadsmod/` and `Lcom/threadsmod/` class; exactly one mod-namespace author-ID
and already-blocked call in the reviewed bridge methods; the native callback
interface; literal-true scheduler handoff before the invocation-only bridge
catches; direct callback effects; accepted-enqueue closure; uncertainty
quarantine; and zero real callbacks outside `Delivery.run`. It additionally
requires the one combined modal, request factory without a click/editor class,
persistent checkbox, report-always and Block-optional routes,
Activity/viewer-bound queue authority, SQLite v2 migration, and retired-UI
absence. Source/template or JADX appearance is insufficient.

The same proof now binds the complete scheduler-release body: lock and catchall
monitor topology, owner-token and zero-token guards, every state clear, viewer
format, live Activity/session/viewer identity, manual-queue priority,
`resumeAuto` plus enabled-state gating, and exact current-context requested-delay
versus changed-context zero-delay drains. The current resolution pins a
275-fixture matrix: one positive and 274 exact negative drifts. The three added
drifts inject private media lookup, displayed-author lookup, and username access
into a mod-owned class and must fail with distinct closed codes. The older
272-case runs and hashes below remain explicitly historical r24 evidence; they
cannot validate r28 or a newly signed artifact.

## Failed signed-evidence wrapper attempt

`work/patchlet-cache-id-diagnostics-20260901-c` is a failed revision-23
pristine replay. It froze source APK SHA-256
`0f6b4515902f78ea194b1bc0563bfcab07f2559d8e80cf6f329d3d1ef786487e`,
resolution-file SHA-256
`b85890e036e56a9ee5b192c5620c8c20650f0034f3daf488790702eb9beba120`,
resolution-directory SHA-256
`80944a4e85fd7e93c3a8541b6f60f8ff63f0eaf62924bfef6238388c14a66178`,
and canonical patchlet-tree SHA-256
`e5e4ea366a0596dd4d80e96a361bec2cf2ae949fb064453b4338f7a0d6139b38`.
Those bindings describe that failed run only.

Its complete decoded-tree idempotency proof passed over 115,711 files, 7,821
directories, and 123,532 entries. Before and after SHA-256 both equal
`3d4f484d52692208c29fb8dd63ce0b01fe360b3e0442b7a96eeaf5d6aae70ca1`;
all five inline rewrite controls were content-identical no-ops on reapply. The
pipeline rebuilt, aligned, and v2-signed a run-local 81,309,292-byte candidate
whose supplemental SHA-256 is
`fcc0dbe69a57e27761fcffa33d5d47b000b2a534ba457bc29cd2bd6bda35da37`
and whose one signer certificate SHA-256 is
`317e3f3813f3b1ec324717faf1bb78f954f49a12122181a85b42338ca10dd079`.

The frozen signed-Dex inspector itself passed that candidate: all raw private
seam definitions, caller provenance, and checks were exact, including one
reviewed author-ID caller. Its automatic caller topology reported five reviewed
terminal routes, which is also the value required by the unchanged positive
fixture. A leaf-by-leaf evaluation of the release wrapper's 136 top-level
failure predicates found exactly one mismatch: `Test-PatchedApk.ps1` still
compared `reviewedTerminalRoutes` with the obsolete literal four.

The build report, SHA-256
`a4802d194558842c921a541f885b427f3481e6bdc03290bd7a8ac99f20273b8d`,
records `status: failed`, `artifactProduced: false`, `failedStage:
release-gates`, no publication move, and `runtimeValidation: not-run`.
`release-check.json` was not promoted and the intended destination remained
absent. The idempotency report SHA-256 is
`9a93f010f729ffd9d8573974b50f3e0f9b1aaa60efcf14e20e6bc83fd1ce7c04`.
The signed run-local bytes remain revision-23 failure evidence only.

Historical patchlet 090 r24 aligned the wrapper expectation to the fixture-proven value
five and AST-binds that duplicated numeric contract between the wrapper and
positive fixture harness. It does not change or weaken the frozen inspector or
its 272 fixtures, and it does not retroactively validate the r23 candidate. The
fresh r24 replay recorded below validates separately rebuilt bytes.

### Focused bridge-flow verifier checkpoint

`work/dex-bridge-flow-canonical-272-a/report.json` records `passed` for all 272
exact cases on 2026-09-01. Its SHA-256 is
`266a9817b425870e7df6893a0d5b7695e7d18ac1eacbb7ac6258f1ffaae555b4`.
The pinned inspector, assembler, harness, and complete fixture-tree hashes are
`937269c9d989f36148f6d24bb68343d3963a419f57a5d78a4d9df82c08771cd2`,
`df2ef57f632432faf7f619e156519da928a3e7f7f0ec797c637320cfdfa2f38b`,
`cc55352da9913c2ec234516b47efe3a1ac159a3dd6a8683cd554c39d0be7affa`,
and `dc7a56b6ba8f38b89ee201bf2edf5f266d4ac945053221c76b37f9449fdf236e`.
The frozen full-matrix collector at
`work/dex-bridge-flow-collect-272-c/results.json` also records 272/272 exact,
zero mismatches, and an unchanged inspector hash before and after. Independent
adversarial review found no blocker in the frozen verifier/fixture scope,
including Fetch handler-tail entry, all five accepted-enqueue loops, scheduler
release branch/identity/delay drift, and quarantine viewer/target identity.

The previous 32-case checkpoint is historical and superseded. This remains
focused tool/fixture proof only; the failed `-b` run never invoked the harness
against its signed candidate because parameter binding failed first. The fresh
revision-24 release run below executed the same hash-bound 272-case harness.
Runtime validation remains pending.

## Historical revision-24 pristine replay and publication

This is a **post-publication documentary append**. Run
`work/patchlet-cache-id-diagnostics-20260901-d` froze the source, resolution,
and complete canonical `patchlets/**` tree before this success evidence was
written. The frozen hashes below remain the release authority; the current
documentation-containing trees are expected to hash differently.

The run started from pristine source APK SHA-256
`0f6b4515902f78ea194b1bc0563bfcab07f2559d8e80cf6f329d3d1ef786487e`.
Its build report records `status: passed`, `artifactProduced: true`, and
publication to
`dist/ThreadsMod-CloneBlocker-415.0.0.26.77-arm64-v8a-cache-id-diagnostics.apk`.
The published APK is 81,309,292 bytes with SHA-256
`042c780ff15f9f2a5a94261eb7a6f0a989d7c3f91f19fa98b71a21c3d7d372fb`.
It is package `com.threadsmod.barcelona`, version `415.0.0.26.77`
(`versionCode 508504469`, minimum/target SDK 28/36).

The exact frozen input bindings were:

- resolution file SHA-256
  `64a1f7ea2fdf83d6a717791ea34567146bf746449d3131c9824510b65907e98b`;
- complete resolution directory SHA-256
  `38ea27743cd74e82de96ab235d2483f148ff889388dac543b089f883f59acb81`;
- complete canonical `patchlets/**` tree SHA-256
  `85c95be44ba2cfc6692d1622a105840f292be2c0f958b502f00e20218f9398af`.

Pre-report, pre-publication, and post-publication freeze checks all matched and
reported zero tree-monitor events. Complete decoded-tree reapplication passed
over 115,711 files, 7,821 directories, and 123,532 entries. Before and after
SHA-256 both equal
`d6a7414bf1ae7c5949a15a641afe68ffe8df97fe305060d4842e6e2cb42c4343`.
All five inline-control rewrites and every other owned operation were no-ops on
reapplication.

The signed release checks passed 12 root and 13 total DEX payloads, 62,235
primary-DEX method references, unique archive inventories, 16 KiB alignment,
and one APK Signature Scheme v2 signer whose certificate SHA-256 is
`317e3f3813f3b1ec324717faf1bb78f954f49a12122181a85b42338ca10dd079`.
All 11 native libraries were preserved; 1,180 assets were byte-compared, with
only the reviewed reassembled `assets/longtail/classes.dex` exclusion. The
exact Report POST proof, all 272 bridge-flow fixtures, the signed-candidate
bridge contract, and all 16 targeted-JADX recoveries passed.

The build, release, and idempotency report SHA-256 values are respectively
`6929d3c842719d91ffa394145d3b2e110908b84cb6afe672c9ed510baab0de29`,
`20e0c4a885d79f0be1163f832a8e5c084208f81aa0c0e9c213fea4b16a891805`,
and `4c8bd297ab196a317b9a799d435bb2843d62b8c9502baa0a0cd94930e5aead56`.
Runtime validation remains `not-run`: the SDK `adb` found no attached device or
emulator, and no installation, login, live mirror/report request, or real
account Block action was performed.

## Historical report/limits replay (Activity known broken)

This is a **post-publication documentary append**. Run
`work/patchlet-report-limits-20260831-d` froze the source, resolution, and
canonical patchlet inputs before this evidence file and `patchlets/README.md`
were updated. The frozen hashes below are therefore the release evidence. The
current documentation-containing resolution directory and `patchlets/**` tree
are expected to hash differently and must not be represented as the frozen
release inputs.

The pristine replay started from source APK SHA-256
`0f6b4515902f78ea194b1bc0563bfcab07f2559d8e80cf6f329d3d1ef786487e`.
Its `build-report.json` records `status: passed`, `artifactProduced: true`, and
publication to
`C:\src\threadsmod\dist\ThreadsMod-CloneBlocker-415.0.0.26.77-arm64-v8a-report-limits.apk`.
The published APK has package `com.threadsmod.barcelona`, size 81,305,196 bytes,
and SHA-256
`f9a26ea6aefc64443dec8e9f6c1d973194aa44bd58014b213f326db576b3a43c`.

The exact frozen input bindings were:

- resolution file SHA-256
  `f6c6f683efa3a2a8eaed4f816cc52b52aa68886099d6d682cb48ebdc223df8f5`;
- complete resolution directory SHA-256
  `b20150f6243d3e6a8f087a500b4e4af558302739636eee57d04cbdd6dbd78710`;
- complete canonical `patchlets/**` tree SHA-256
  `4d60ef989315548a9523302b15729fa486d036837f05e5984bb5fa45d90124af`.

The pre-report, pre-publication, and post-publication frozen snapshot checks all
matched, and every associated monitor reported zero events. Full decoded-tree
reapplication remained unchanged at SHA-256
`91650c8bf2d3a3a3f1e3e6193863abf05b9928917d0a44fbffcb986ad3eea3c0`,
covering 115,711 files, 7,821 directories, and 123,532 entries. Its no-op counts
were 54 identity, one Settings UI, 181 generated Smali, three bridge, two inline
templates, two Report templates, three Settings templates, two hooks, one
drawer Settings, and four inline-control operations.

The release checks passed with 12 root DEX and 13 total DEX payloads, 62,212
primary-DEX method references, unique archive inventories, 16 KiB alignment,
and one v2 signer whose certificate SHA-256 is
`317e3f3813f3b1ec324717faf1bb78f954f49a12122181a85b42338ca10dd079`.
All 11 native libraries were preserved. The asset gate compared 1,180 files and
retained only the reviewed reassembled `assets/longtail/classes.dex` exclusion.
The direct DEX proof found exactly one reviewed Report POST call in
`ReportClient.post`: literal offset 80, call offset 82, argument register 1,
and zero alternate control-flow entries. All seven targeted-JADX recovery
targets also passed.

Runtime validation for that replay was `not-run`. A later isolated exact-Dex
negative control reproduced a fatal Activity construction crash in these
published bytes, so run `d` and its APK are retained as historical static
evidence and do not authorize the current install recommendation. Historical
runs `a`, `b`, and `c` remain failed/non-publishing evidence.

## Release-contract evidence

- The local JADX 1.5.6 all-in-one JAR is pinned at SHA-256
  `fe3e12c45acf75f92369685fd02d1d7a7323385dc725680a9b98a0dac0ea554b`.
  The separately pinned `TargetedJadxRecovery.java` helper loads a candidate once
  and writes only the exact resolution target set. A focused probe against the
  preceding signed APK recovered `ModBootstrap` and `ThreadsBlockBridge` and
  matched all reviewed runtime/bridge strings. A focused recovery from the
  then-current compile-only DEX also recovered `ReportClient` and matched
  `ReportStore.reserveNextDue(` and `ReportEndpoint.writeUrl()`. Targeted JADX is
  readable seam evidence, not the authoritative HTTP-method proof: pinned JADX
  1.5.6 renders the compiled literal call as
  `setRequestMethod(TigonRequest.POST)` even though the signed DEX contains no
  field read there. Patchlet 090 revision 14 instead binds the exact
  `ReportClient.post` owner/callee descriptors and proves one immediate
  `const-string "POST"` into the exact HTTPS call argument register, with no
  alternate branch, switch, or handler entry into the invoke. Revision 15
  retains that proof while adding the Activity construction contract.
- Source-versus-pre-report artifact inventory found 1,181 `assets/**` files on
  each side and exactly one byte difference: `assets/longtail/classes.dex`.
  Apktool `--all-src` reassembles that DEX. The resolution therefore excludes
  only that exact file from byte comparison, requires it on both sides, and binds
  it to the existing all-DEX magic/header/checksum/signature gate. The other
  1,180 assets and every `lib/**` entry remain exact hash comparisons.
- `work/release-contract-test-20260831-c` deliberately supplied a wrong input
  digest. The focused test passed: `build-report.json` retained source and
  resolution-file hashes, set `artifactProduced` to false, left output and
  published fields null, and created no `dist` file.
- Passed release/build reports carry one explicit binding object for source APK,
  resolution file, and output APK SHA-256. At this checkpoint a revised signed
  artifact still awaited pristine replay; the later authoritative replay is
  recorded below.

The pristine `work/patchlet-report-limits-20260831-c` attempt reached the signed
candidate but failed closed at the earlier decompiler-text POST check. Its
`build-report.json` records `failedStage: release-gates`,
`artifactProduced: false`, a null output binding, no publication attempt, and a
clean publication rollback. The quarantined staged candidate was never copied
to `dist`. Direct Smali inspection at `ReportClient.smali` lines 2208–2210
showed `const-string v1, "POST"` immediately followed by the HTTPS invocation
using `v1`. After the revision-14 gate was added, the focused signed-candidate
probe `work/revised-release-gate-probe-20260831-a-report.json` passed with one
match in `classes.dex`, literal/call code offsets 80/82, argument register 1,
and zero alternate entries. Five synthetic fixtures separately proved that an
actual GET with a POST decoy, a call in another method, duplicate calls, and a
branch-merge argument all fail. These probes justify the verifier correction;
they do not publish the quarantined candidate or replace a fresh exact-resolution
replay.

## Activity construction correction

The previously published report/limits APK is retained as historical static
evidence but is not the current install recommendation. Launching its exact
signed `classes.dex` in the isolated API 37 x86_64 16 KiB probe reproduced a
fatal `NullPointerException`: `CloneBlockerActivity.statCard()` called
`View.setLayoutParams(null)` while building Overview, before the Activity had
reached `setContentView`. This exactly explains the reported empty/terminated
Activity window.

Patchlet 050 revision 7 removes that call, attaches the visible shell before the
state snapshot, renders corrupt local state as a fail-closed review card, and
leaves the explicit weighted parameters owned by the two parent `addView`
calls. Patchlet 020 revision 7 also routes public status, explicit snapshot
status, normal updates, and Disable through `status_threads_<viewerId>` or
`status_general`; the retired raw `status` key is no longer read or written.
Patchlet 090 revision 16 checks these canonical method bodies and retains the
Activity to signed targeted-JADX recovery with ordered shell/snapshot evidence
and a forbidden `setLayoutParams(null)` string while adding the exact
single-control, Report-handoff, and removed-copy contracts.

The separately SHA-pinned `Test-ActivityUiEmulator.ps1` copies the candidate's
exact primary DEX into a no-permission probe package, verifies DEX equality,
launches Activity and Settings, checks required visible labels and crash logs,
and removes only `com.threadsmod.activityprobe`. Its negative control against
the old published APK failed because Activity did not remain present/resumed,
as expected. The unchanged manifest/drawer/inline rewrite sets remain bound to
the same exact source SHA and were re-attested by the fresh pristine replay
below; no host rewrite or obfuscated Threads seam changed in this correction.

## Historical Activity-corrected replay and probe

`work/patchlet-activity-fix-20260831-a` replayed the complete eight-patchlet
series from pristine source APK SHA-256
`0f6b4515902f78ea194b1bc0563bfcab07f2559d8e80cf6f329d3d1ef786487e`.
Its `build-report.json`, `release-check.json`, and `idempotency-check.json` all
record `passed`, and the build report records `artifactProduced: true`.

That historical separate-ID APK is
`dist/ThreadsMod-CloneBlocker-415.0.0.26.77-arm64-v8a-report-limits-activity-fix.apk`,
size 81,305,196 bytes, SHA-256
`61b9978e59c56adccc3375ac49e74611a15e42d0ffa472be2765eea0e6bd58f4`.
It is package `com.threadsmod.barcelona`, version `415.0.0.26.77`
(`versionCode 508504469`, minimum/target SDK 28/36), with one v2 signer whose
certificate SHA-256 is
`317e3f3813f3b1ec324717faf1bb78f954f49a12122181a85b42338ca10dd079`.

The frozen release bindings are:

- resolution file SHA-256
  `8b2456d07797d355a120bdfb1e8986172f6e736e7704e8bc64a29ce58e4b7a3a`;
- complete resolution directory SHA-256
  `ac7e1154d9efbbf62d36a69e239ebe892319eb10603b1d76348e5a36015913be`;
- complete canonical `patchlets/**` tree SHA-256
  `a40f023fb9983603550346c045b2cd3c13f2755e7019e7bae227085d91779758`.

Full decoded-tree reapplication remained content-identical at SHA-256
`743b0b10b3c63fdeca52a3706f9dcf9d94d5d6584ca68186f38883d36f33430f`,
covering 115,711 files, 7,821 directories, and 123,532 entries. No-op counts
were 54 identity, one Settings manifest, 181 generated Smali, three bridge, two
inline templates, two Report templates, three Settings templates, two hooks,
one drawer Settings, and four inline-control operations.

Release checks passed 12 root/13 total DEX payloads, 62,213 primary-Dex method
references, unique archive entries, 16 KiB alignment, signature/signer
continuity, all 11 native libraries, 1,180 byte-compared assets plus the exact
reviewed longtail-Dex exclusion, the direct signed-Dex Report POST proof, and all
eight targeted-JADX recoveries. The added Activity target requires
`setContentView(this.scroll)` before `ModStateStore.snapshot(this)` and forbids
`setLayoutParams(null)`.

The final isolated report at `work/activity-ui-probe-fixed-20260831-c.json`
passed cold launch, resumed-state, visible-label, and crash-log checks for both
Activity and Settings on an API 37 x86_64/16 KiB emulator. Candidate and probe
primary DEX SHA-256 both equal
`b5caa546656af25a576ef16550659fe6b1715405ea81c89d7887abf819fbc738`.
The probe package requested no permissions and records
`networkOrAccountActionAttempted: false`.

After publication, the probe expectation was corrected to the clean disabled
label `Enable & sync` and its UI-XML comparison was escaped. The corrected probe
tool SHA-256 is
`e0a2a67c06fea6398059ad9436797a9a5fdbeadbb541847d53987ba4973c35e6`,
the manifest SHA-256 remains
`31fedc8103d75430d431716e8e79fbbed939c8c4865db1a797be418398740dbf`,
and the then-current resolution-file SHA-256 was
`59fbc66d8160de762d32c059d74aa873b5dfc786af683dcd435a7f542681f988`.
This repin changed neither production Java nor the published APK; the frozen
run bindings remain authoritative for those bytes.

The full arm64 clone was not installed, logged in, or allowed to perform a
mirror read, report write, or account action. This probe proves exact-Dex
private-screen construction only, not native drawer/action-row integration,
host rendering on all devices, Meta backend acceptance, or real Report/Block
behavior.

## Historical single-action compact replay

`work/patchlet-single-action-compact-20260831-a` replayed the complete
eight-patchlet series from pristine source APK SHA-256
`0f6b4515902f78ea194b1bc0563bfcab07f2559d8e80cf6f329d3d1ef786487e`.
Its `build-report.json`, `release-check.json`, and `idempotency-check.json` all
record `passed`, and the build report records `artifactProduced: true` plus a
transactionally committed `dist` path.

That historical separate-ID APK is
`dist/ThreadsMod-CloneBlocker-415.0.0.26.77-arm64-v8a-single-action-compact.apk`,
size 81,301,100 bytes, SHA-256
`f64aa8478edf8c70b53859482690d81ef2313139f2b216dbccbe0e5f928f2831`.
It is package `com.threadsmod.barcelona`, version `415.0.0.26.77`
(`versionCode 508504469`, minimum/target SDK 28/36), with one v2 signer whose
certificate SHA-256 is
`317e3f3813f3b1ec324717faf1bb78f954f49a12122181a85b42338ca10dd079`.

The frozen release bindings are:

- resolution file SHA-256
  `da0f025ff23c1d47381c2afb66ea151ceb2c2506e2d040bf2ef71f19711cc225`;
- complete resolution directory SHA-256
  `4f3a5fddd3e952319e75343f8acbb11e8caeff32d86db64d3d4abaa022460fd3`;
- complete canonical `patchlets/**` tree SHA-256
  `53c0e5ad9a8463a714bd2ede63002258d62cc2e4357871e71544ff12d005a13b`.

Full decoded-tree reapplication remained content-identical at SHA-256
`21f62c29601544f37bea6b15a7b177b9ca08ef1f768ed2abfe7c6445179a987f`,
covering 115,713 files, 7,821 directories, and 123,534 entries. No-op counts
were 54 identity, one Settings manifest, 183 generated Smali, three bridge, two
inline templates, two Report templates, three Settings templates, two hooks,
one drawer Settings, and four inline-control operations.

Release checks passed 12 root/13 total DEX payloads, 62,216 primary-Dex method
references, unique archive entries, 16 KiB alignment, signature/signer
continuity, all 11 native libraries, 1,180 byte-compared assets plus the exact
reviewed longtail-Dex exclusion, the direct signed-Dex Report POST proof, and
all nine targeted-JADX recoveries. Recovered `X.0sC` contains exactly one
`InlineActionRowAdapter.render` and no Report renderer/factory call. The
Report action factory constructs exactly one request and click; the click calls
only `ReportController.showFromForeground` once and contains no delivery,
outbox, Block bridge, or manual-scheduler path. Final DEX forbids
`threadsmod_inline_report`, `Safe processing`, `Before exact review`, and
`tree55.com`.

Runtime validation for this exact APK is `not-run`. The Activity probe above
belongs to the historical Activity-fix candidate and was not rerun against
these bytes. No full arm64 install, login, mirror read, Report delivery, or
native Block action was performed.

## Historical resolved-inline replay (pre-cache-placeholder)

This section is a **post-publication documentary append**.
`work/patchlet-resolved-inline-20260831-a` replayed the complete eight-patchlet
series from pristine source APK SHA-256
`0f6b4515902f78ea194b1bc0563bfcab07f2559d8e80cf6f329d3d1ef786487e`.
Its `build-report.json`, `release-check.json`, and `idempotency-check.json` all
record `passed`; the build report records `artifactProduced: true` and the
transactionally committed `dist` path.

That historical separate-ID APK is
`dist/ThreadsMod-CloneBlocker-415.0.0.26.77-arm64-v8a-resolved-inline.apk`,
size 81,301,100 bytes, SHA-256
`93c521d4f9b4d709d5582d91e1f4b7f9f567751d458c13ecba0f996a9a370192`.
It is package `com.threadsmod.barcelona`, version `415.0.0.26.77`
(`versionCode 508504469`, minimum/target SDK 28/36), with one v2 signer whose
certificate SHA-256 is
`317e3f3813f3b1ec324717faf1bb78f954f49a12122181a85b42338ca10dd079`.

The frozen release bindings are:

- resolution file SHA-256
  `3d9a9467844e04002b6d87c846d9bb88778d002a272d75901b68309880884162`;
- complete resolution directory SHA-256
  `6f82f017e03e45ff0e88cb48d874602ad35446f3b9cf8e399bdc2ae785d97b54`;
- complete canonical `patchlets/**` tree SHA-256
  `5189d6834fc06e0d614c1d3b556cf2161599da76577230043149b0632911a343`.

Full decoded-tree reapplication remained content-identical at SHA-256
`2669b323aaaa434aaa635f7070a5a143e9c95ee59a24b19f5b4ef2932c35afea`,
covering 115,714 files, 7,821 directories, and 123,535 entries. No-op counts
were 54 identity, one Settings manifest, 184 generated Smali, three bridge, two
inline templates, two Report templates, three Settings templates, two hooks,
one drawer Settings, and four inline-control operations.

Release checks passed 12 root/13 total DEX payloads, 62,229 primary-Dex method
references, unique archive entries, 16 KiB alignment, signature/signer
continuity, all 11 native libraries, 1,180 byte-compared assets plus the exact
reviewed longtail-Dex exclusion, the direct signed-Dex Report POST proof, and
all 13 targeted-JADX recoveries. Signed recovery proves the opaque row-model
handoff through click/controller/scheduler, exactly one manual
`ThreadsBlockBridge.blockResolved` call, the retained hintless/automatic
`ThreadsBlockBridge.block` path, `model_id_mismatch` refusal, and exact GitHub
raw -> jsDelivr -> AWS read ordering. Final DEX forbids
`threadsmod_inline_report`, `Safe processing`, `Before exact review`, and
`tree55.com`; the exact AWS `/v1/reports` URL remains the only write route.

Runtime validation for this exact APK is `not-run`. The Activity probe above
belongs to the historical Activity-fix candidate and was not rerun against
these bytes. No full arm64 install, login, live mirror read, Report delivery,
or native Block action was performed. The static release proves the redundant
inline lookup is bypassed when the exact same-row model is available; it does
not prove live Meta acceptance. It predates the current session-cache
placeholder fallback, bounded chooser username handoff, diagnostic class, and
final-Dex legacy lookup forbiddance, so it is not release evidence for this
resolution revision.

## Historical replay evidence

The earlier baseline form of this resolution was replayed from the exact source APK in
`work/patchlet-e2e-20260831-a`:

- the mandatory no-change Apktool rebuild passed before patching;
- 54 identity and two lifecycle rules applied with exact counts;
- 50 generated Smali files and three template-rendered bridge files were
  installed; the generated compile-only bridge stub was excluded;
- reapply changed zero of 89 owned files, whose aggregate SHA-256 stayed
  `232292a50658865b883968270f2be12165b40b12af1b89bf10abb5b9aa2b8839`;
- all 13 packaged DEX payloads passed header checks, with 61,315 primary method
  references;
- the final package, mirror/write boundary, blocked-origin, alignment, archive,
  one-v2-signer, signer-continuity and native-library gates passed.

The replayed APK SHA-256 is
`a3e9c0374b5c4d9643683d2d40f710edf74eabe916ee8af5eead0ec3d1e6ce08`.
That run predates patchlets 050 and 060 and does not validate the current drawer,
rich dialog, or sibling-action topology.

The pre-report drawer/rich-inline resolution was replayed from the exact pristine
source APK at `work/patchlet-drawer-rich-inline-20260831-a`:

- the mandatory no-change Apktool round trip passed before patching;
- exact counts applied: 54 identity, one Settings manifest, two lifecycle, one
  drawer, and four inline rules;
- 102 generated runtime Smali files, three bridge templates, two inline
  templates, and three drawer templates were installed;
- reapply changed zero of 150 owned files; aggregate SHA-256 remained
  `8a8812d02f34f14be02ad03762e8fd3168b82d096c8a2d8aaca7ddef63f5d745`;
- all 13 DEX payloads passed, with 61,757 primary DEX method references;
- package/activity privacy, drawer and post-Share sibling hooks, rich-dialog
  strings/safety, mirror and forbidden-origin policy, archive, 16 KiB alignment,
  signer continuity, and native-library preservation passed.

That historical candidate is
`dist/ThreadsMod-CloneBlocker-415.0.0.26.77-arm64-v8a-drawer-rich-inline.apk`,
size 81,260,140 bytes, SHA-256
`55f7daedc524dbb10a9686d6438da04d25afb266a871a65ad064ace21b471735`.
It predates patchlet 070 and configurable-limits completion. It is not evidence
for the current report transport, SQLite outbox, former Report-sibling revision, or final limits
contract and must not be published as the current release.

## Update rule

An AI updater may reuse a symbol only after the proof in `resolution.json`
matches the newly decoded APK. The drawer row/modifier/key, both inline topology
anchors, spare-register liveness, the exact-host snapshot helper, immutable row
identity, media/author/ID/username capture, shared row snapshot with derived
Block and `ReportRequest` values, opaque media handoff, report caption and
permalink accessors from the same captured media, the live sixth-String request
slot, sole final-field sanitizer write, request/controller/client queue guards,
returned-root `targetUrl`, one initialized/attached sole-entry evidence array,
one combined-modal route, initiating Activity/viewer binding,
report-always/Block-optional action flow, persistent checkbox ownership,
ReportStore v1-to-v2 migration, retired report-class absence, absence of raw
private seams from stable templates, session user-cache factory/get-or-put path,
one-control topology, callback semantics, exact automatic/manual/bridge caller provenance,
exact ownership of every resolution-supplied private seam call, native
callback-interface identity, every bridge/native dispatcher route and its
caller provenance, the delivery-only real-callback topology, the narrow bridge
preparation/dispatch/result topology, release-tool caller/callee AST parameter
compatibility, the exact resolution-bound 28-argument DEX bridge-inspector guard,
the separate 21-argument permalink-inspector guard and ordered wrapper/harness
member bindings, the 275 bridge and 20 permalink fixture counts, the ordered
exact 28 targeted-JADX classes including `ModStateStore` and strict-sanitizer
`ReportValues`, the revision-35 raw signed-DEX Application bootstrap flow, the
revision-36 proxy wrapper/harness/resolution AST contracts, the revision-37
proxy status/lifecycle proof, and mutation seam
require digest-specific human review. If a proof or exact rewrite count drifts,
the patchlet must stop and the resolution must be reviewed rather than weakening
a check or applying a global package-name replacement. A new resolution must
also bind and pass both signed-Dex verifiers and their fixtures; targeted JADX
stays alias-free secondary evidence and cannot waive either gate.
Patchlet 090 r31 retains r28's exclusion of ambiguous `Send report` from `forbiddenDexStrings`: exact pristine `decompiled/apktool-3.0.3/smali/X/0Dw.smali:1238` contains `Send reports blocking`, and `DexInspector` performs case-insensitive substring matching. This is a false-positive collision, not evidence of a dedicated Report button. Exact one-control and zero-separate-control counts, legacy renderer/tag absence, retired `InlineReportClick`/`ReportDialog` class absence, targeted-JADX host topology, and signed-Dex flow remain authoritative.

## SOCKS5 revision-34 collision review

The first SOCKS5 release replay, `work/patchlet-socks5-proxy-20260902-b`, matched source APK SHA-256 `0f6b4515902f78ea194b1bc0563bfcab07f2559d8e80cf6f329d3d1ef786487e`, applied the complete series, and proved a content-identical 123,538-entry reapplication at SHA-256 `5b15b1d74f9a20c363b60ded3706e6b159a9f9a6cb5d677a8ac8b94bd38a002e`. It then produced a signed run-local candidate and stopped at `release-gates`: the case-insensitive broad DEX scanner reported `socksProxyPort`. Its build report SHA-256 is `573d303ca139a5a1fc0e7a0f857024b782779f8b2651d0e8d3989dd6afbd4552` and records `status: failed`, `artifactProduced: false`, a null output binding, no publication move, verified cleanup, and runtime `not-run`. The candidate is failure evidence only and was not copied to `dist`.

The same failure reproduces against the untouched source APK. Exact pristine `decompiled/apktool-3.0.3/smali_classes11/com/facebook/proxyservice/observer/ProxyServiceBroadcaster.smali:98` contains `.method public final declared-synchronized getSocksProxyPort()I`; that file's SHA-256 is `f04b40261dfe51dc90a84cff664ce5a550c9f090468c0223393a6dc8c8c62bac`. `DexInspector` performs case-insensitive raw substring matching, so lowercase `socksProxyPort` matches the `SocksProxyPort` suffix of that host method. Canonical proxy Java contains no exact lowercase key or Java fallback API.

Revision 34 removes only `socksProxyPort` from the broad list and adds exact-SHA semantic proof `proxy-pristine-socks-port-substring-collision`. All nine signed proxy targets now independently forbid the exact-case set `allowBypass(`, `System.setProperty(`, `ProxySelector.setDefault(`, `socksProxyHost`, `socksProxyPort`, and `java.net.useSystemProxies`; canonical Java is scanned under the same exact semantics. The other 22 broad forbidden DEX strings are unchanged and remain blocking.

Review-required validation passed before promotion:

- catalog report `work/catalog-socks5-r34-review-20260902-a.json`, SHA-256 `8e8d80d7365c3298d1365020e33246d5c3cb597d15fb3fccc040ea56254e6651`;
- pristine resolution report `work/resolution-socks5-r34-review-20260902-a-pristine.json`, SHA-256 `f83e7c521160f862c82cb197c2bce75333dd4e28ffb895a6606f2aaf85460d2a`;
- already-patched resolution report `work/resolution-socks5-r34-review-20260902-a-patched.json`, SHA-256 `cae7e2161b8970b610126efbf68bab8de8583e18d331e6339536033f4d6d1f50`;
- host policy report `work/host-socks5-r34-review-20260902-a-report.json`, SHA-256 `331af6fafe4b3aef5d6b6a31df1d9f810186464f1457384a2b48a9c67dd6efc5`.

These checks do not promote the resolution or publish an APK. A passing hash-bound release-contract run, human review, promotion to `verified-current`, a fresh pristine pipeline, and the isolated Activity probe remain required.

## SOCKS5 revision-35 bootstrap-flow correction

The fresh revision-34 replay at
`work/patchlet-socks5-proxy-20260902-c` matched the exact source APK and passed
complete-tree no-op reapplication. The decoded tree was content-identical
before and after at SHA-256
`b9cccba85b7c5d0125a7332bfc86d1d64cb6b8ef7604d47b073162f01ea65a6d`,
covering 115,714 files, 7,824 directories, and 123,538 entries. The
`idempotency-check.json` file SHA-256 is
`a1a3e5a0e91a1a9c19ad06078cf13ba851afb4dd0884b0c0fb8a58aafd65297f`.

That run rebuilt, aligned, and signed a run-local candidate with SHA-256
`3d0d8615b01443d0bd97b52a319df3ad5829e09631187725bd24cced6bf190b6`,
then stopped fail-closed at `release-gates`. Default targeted JADX skipped all
555 instruction units of
`BarcelonaAppShell.attachBaseContext(Context)` and emitted a
`Method not decompiled` placeholder, so the readable recovery could not find
`super.attachBaseContext(`. The build report SHA-256 is
`cb2bdb55eb9a4a65c76af15be185286d0851b29b06b3a1820b8780565f7b4801`;
it records `status: failed`, `artifactProduced: false`, no publication move,
verified rollback, and runtime `not-run`. The run-local APK is failure evidence
only and was not copied to `dist`.

Revision 35 made the raw signed-DEX bootstrap-flow verifier authoritative.
For the exact Application owner and `attachBaseContext(Context)` method, it
must prove the reviewed receiver and incoming-context entry moves, exact
superclass call, immediately adjacent `ProxyBootstrap.install(Context)` call,
resolution-owned original next host-field read, and reuse of the same live
context register. It must also prove the bootstrap call has exactly one caller
across the complete signed DEX inventory, no alternate entry reaches the
reviewed entry/super/install sequence, no try range covers that sequence, and
the complete proxy-fallback absence contract still passes.

`TargetedJadxRecovery` with `setShowInconsistentCode(true)` is secondary
readable evidence only. The signed target must still reject `Method dump
skipped` and the exact `BarcelonaAppShell.attachBaseContext` `Method not
decompiled` marker; recovered source cannot replace or waive any raw flow
condition. No fresh pristine r35 replay was completed, so this correction does
not retroactively validate the failed revision-34 candidate. Those bytes cannot
be reused or published.

## SOCKS5 revision-36 release-contract AST hardening

Revision 36 makes no production-tree, decoded-rewrite, proxy-policy, or runtime
change. It hardens the release-only evidence boundary around the revision-35 raw
bootstrap verifier. PowerShell AST must bind the signed wrapper and its positive
fixture harness to the resolution's exact numeric literals for 13 bootstrap
fixtures and 11 bootstrap-inspector arguments. Each literal must occur exactly
once in its reviewed role, and both calls must carry the same ordered ten
semantic bindings after the APK argument. The proxy negative-contract fixtures
must reject missing, duplicate, nonliteral, divergent, or reordered forms.

The resolution must also expose one ordered exact 28-class targeted-JADX
inventory, including `ModStateStore`, with no missing, duplicate, extra, or
reordered target. The Application target's `orderedStrings` must be exactly
`super.attachBaseContext(` followed by `ProxyBootstrap.install(`. The raw
signed-Dex bootstrap gate must occur exactly once and before targeted JADX, so
readable recovery cannot run first or substitute for the instruction-level
proof.

These are required static contracts, not successful-candidate evidence. The
failed `work/patchlet-socks5-proxy-20260902-c` run remains unpublished r34
failure evidence, and neither r35 nor r36 retroactively validates it. No fresh
pristine r36 replay was completed.

## SOCKS5 revision-37 status and lifecycle proof

Patchlet 080 revision 3 corrects production status seams. Service and controller
state must distinguish four topologies: `paused_guard_active` is a newly
established full-route no-consumer guard; `paused_guard_retained` is the prior
full-route guard retained after replacement refusal. Both are shown **Paused**
with app traffic blocked. `unprotected_prior_routing` is an older forwarding TUN
retained and shown **Unprotected** with the warning that earlier numeric DIRECT
exclusions may remain. `paused_vpn` is exclusively no TUN and is shown
**Unprotected**, inactive, with direct traffic possible. Retained forwarding is
not a fresh blackhole and must not be reported as connected. Enabled
configuration with runtime `disabled` must also map to explicit **Unprotected**,
inactive, direct-possible status instead of stale **Starting** after revoke,
service destruction, or notification disconnect.

Proxy Settings must independently poll the credential-free runtime token every
500 ms only while the Activity is resumed. The full status is rebuilt only when
that token changes; pause cancels polling. Both the initial and recurring
`Handler.postDelayed` boolean results are checked. Either rejection must stop
polling and visibly show **Unavailable**, so stale status cannot remain an
apparently current claim. This UI poll is distinct from the service's 500 ms
native-worker liveness watchdog and does not imply instantaneous notification.

Revision 37 adds no production or decoded rewrite of its own. Its blocking
canonical-Java, targeted-JADX, and static host checks must prove the exact
four-topology mapping, enabled/runtime-disabled fallback, retained-forwarding
disclosure, polling lifecycle, token-change-only
full refresh, checked enqueues, and visible rejection above while retaining all
revision-36 bootstrap contracts. These are required proof definitions, not a
passing-run claim. A fresh hash-bound r37 release-contract review, human review,
promotion to `verified-current`, pristine pipeline replay, and isolated Activity
probe remain required. There is no device, account, runtime, SOCKS endpoint, VPN
route, or network packet proof, and r37 does not validate the failed `-c` bytes.

### Revision-37 pre-promotion review

The bounded review passed for the exact source SHA-256
`0f6b4515902f78ea194b1bc0563bfcab07f2559d8e80cf6f329d3d1ef786487e`:

- `work/catalog-socks5-r37-preflight-20260902-b.json`, SHA-256
  `64f6354f26595668b40e7de783b5a1bcfc1cadd9bbfb299f1d362634ce6f6a75`;
- `work/host-socks5-r37-preflight-20260902-c-report.json`, SHA-256
  `ad6eb4d78ff6c00112d7b91d40748179f0187b9aaf200fb618cf7b08bed139b3`;
- pristine resolution report
  `work/resolution-socks5-r37-review-20260902-b-pristine.json`, SHA-256
  `b6043024944e4439f266f4a371b05212de4c356a862edbe11339ba5ff99f4aa0`;
- already-patched resolution report
  `work/resolution-socks5-r37-review-20260902-b-patched.json`, SHA-256
  `8c76325d2b363c61539c77a616723613fc57dd758d807efe94825ea39bce8276`;
- `work/release-contract-socks5-r37-20260902-a-report.json`, SHA-256
  `641e0663a82112517dfdf751f38f9d189c0e78c9c080adee0316a7badda75d0d`.

The release-contract report passed the exact 13-case proxy-bootstrap,
275-case Block bridge, and 20-case permalink fixture contracts plus the
release-tool AST, drift, rollback, and publication-order negatives. A separate
fresh D8/JADX preflight under
`work/proxy-jadx-preflight-r37-20260902-c` recovered the three changed proxy
classes and passed every resolution-owned required, exact-count, ordered, and
forbidden string contract. Review retained the existing exact-SHA Application
hook and promoted the resolution to `verified-current` at
`2026-09-02T17:38:27+07:00`; the promoted resolution SHA-256 is
`6c12261780fd2902dac40f9a465ffa27a116869b0aa880f9a676944070d0e702`.

At the promotion checkpoint, promotion was not a release or runtime result: a
fresh pristine pipeline, signed-candidate gates, and isolated no-permission
Activity probe still remained required before publication. The next section
records their later independent completion. Device/arm64 installation, VPN
consent, SOCKS reachability, packet routing, account, and network behavior
remain unproved.

### Revision-37 pristine replay and publication

This is a **post-publication documentary append**. The successful pipeline
froze its canonical inputs before this evidence note and the other release
documents were updated. The recorded frozen inputs remain authoritative; the
current documentation-containing tree must not be substituted for them.

Run `work/patchlet-socks5-proxy-20260902-d` replayed the complete series from
source APK SHA-256
`0f6b4515902f78ea194b1bc0563bfcab07f2559d8e80cf6f329d3d1ef786487e`
against promoted resolution SHA-256
`6c12261780fd2902dac40f9a465ffa27a116869b0aa880f9a676944070d0e702`.
Its canonical `patchlets/**` snapshot was frozen at SHA-256
`8d1f32eb74f19db7fe206d05633938d3be7bd57a676cc24289f31346cc6b2bd3`.
The pre-report, pre-publication, and post-publication freeze checks all passed
with zero retained monitor events.

The release evidence reports are:

- `work/patchlet-socks5-proxy-20260902-d/build-report.json`, SHA-256
  `297384467a62d9df195598b4dfe2e7a31f9231befe3861c3a5db630650236453`;
- `work/patchlet-socks5-proxy-20260902-d/release-check.json`, SHA-256
  `ea42118d099ee71325bd38539cd1b42e87a0095e53cc1e647dd457b7e5fd21da`;
- `work/patchlet-socks5-proxy-20260902-d/idempotency-check.json`, SHA-256
  `296dabf2573702c95a50fa32308db086c8e5cc3b1dce7fae81476c263d71d30b`.

All three reports record `status: passed`; the build records
`artifactProduced: true` and transactional publication. Complete decoded-tree
reapplication was content-identical across 115,715 files, 7,824 directories,
and 123,539 entries, with SHA-256
`ec464c9ada4deb29a82b59d87449fa1f54416ac55bccae174d526ca7d0de65a4`
before and after.

The transaction published
`dist/ThreadsMod-CloneBlocker-415.0.0.26.77-arm64-v8a-socks5-proxy.apk`,
size 81,481,510 bytes, SHA-256
`53f65dc1454f873b2da11548c3550b4bed09aae689abb191c3e5f4b9a8ab82e8`.
It is package `com.threadsmod.barcelona`, version `415.0.0.26.77`, version code
`508504469`, minimum/target SDK 28/36, and has one APK Signature Scheme v2
Android Debug signer whose certificate SHA-256 is
`317e3f3813f3b1ec324717faf1bb78f954f49a12122181a85b42338ca10dd079`.

Release gates passed the 13-case raw proxy-bootstrap, 275-case Block bridge,
and 20-case permalink fixture contracts; all 28 exact ordered targeted-JADX
recoveries; the r37 four-topology and polling-lifecycle proofs; archive and DEX
integrity; original-native-library preservation plus the single pinned arm64
tunnel library; AArch64/16 KiB alignment; signature and signer binding; and the
release-tool, drift, rollback, and publication-order negatives.

The isolated no-permission Activity report
`work/activity-ui-probe-socks5-r37-20260902-a-report.json`, SHA-256
`439b80ddb31424d78a9aa9f076a3bcdc91edaa5ba74a972b209beb7d0c9f54b9`,
records `status: passed`. It repackaged the candidate's exact primary DEX,
launched `CloneBlockerActivity`, `CloneBlockerSettingsActivity`, and
`ProxySettingsActivity` on an API 37 x86_64/16 KiB emulator, found the reviewed
visible labels, recorded empty crash logs, and reports
`networkOrAccountActionAttempted: false`.

`runtimeValidation` remains `not-run`. The isolated UI probe is Java/Dex screen
construction evidence, not a full arm64 host installation or cold start. No
Android VPN consent, live SOCKS5 endpoint or authentication, DNS/TCP/UDP/QUIC
traffic, failure/reconnect case, account/login flow, report delivery, native
Block action, or independent packet capture was exercised. Therefore this
release does not establish absence of direct packets or support an all-Threads-
traffic proxy claim.

## Passive blocking revision-38 review-required resolution

The passive-blocking change is bound to the same pristine source APK SHA-256
`0f6b4515902f78ea194b1bc0563bfcab07f2559d8e80cf6f329d3d1ef786487e`.
The governing catalog revisions are patchlet 020 r14, patchlet 050 r11,
patchlet 060 r10, and patchlet 090 r38. The resolution status is deliberately
`review-required`: the published revision-37 candidate and its 28-class JADX
run predate the passive SQLite index, ten-minute refresh owner, viewport
observer, and visible-only scheduler input, so none of that historical evidence
validates this revision.

### Exact-SHA viewport chain

The pristine decoded source proves this bounded Compose chain:

- `X/A2m.A00(X/8eh, Function1)` wraps the callback in `X/A2n` and appends that
  exact modifier element through `X/8eh.EHK`.
- `X/A2n.A01` creates the corresponding `X/Bul` node and installs the same
  `Function1`; `X/Bul.DNR(X/8hm)` forwards each layout-coordinate event to that
  callback.
- `X/5mg` is the exact three-method remember-observer lifecycle interface:
  `onRemembered`, `onForgotten`, and `onAbandoned`.
- `X/8hm.Cku()` supplies the attached/placed predicate. The exact
  `X/0qD.A02(X/8hm, true)` path resolves root coordinates and carries the true
  clip flag into `X/8hm.D0P`; `X/8ok.A0A()` is the reviewed empty-rectangle
  predicate. On the Windows decoded tree the case-colliding `X/0qD` class is
  canonically stored at `smali_classes2/X/0qD.1.smali`; an updater must not
  substitute the unrelated `smali/X/0Qd.smali` interface.

Patchlet 060 r10 therefore owns one remembered
`InlineVisibilityCallback` keyed by the immutable request and the exact captured
author-model object, one `X/A2m.A00` modifier, and lifecycle removal when the
row is forgotten or abandoned. Every geometry callback first calls the
idempotent memory-only registration API and forwards visibility only when that
registration succeeds; stable Java owns transition deduplication. This lets a
still-remembered row recover after an account-context reset without retaining
the old viewer registration. The rendered callback may pass only bounded stable
strings plus its opaque token to `AutoBlockSync`; it owns no SQLite, blocklist,
bridge, or mutation call. A future APK whose modifier factory,
element/node handoff, callback coordinate type, remember-observer interface,
root-bounds path, or empty-rectangle predicate drifts must fail closed and
receive a new exact-SHA resolution.

### Passive release contracts and current boundary

The resolution now requires `Lthreadsmod/autoblock/BlocklistStore;` and
`Lthreadsmod/inlinecontrol/InlineVisibilityCallback;`. Its targeted-JADX list
has exactly 30 ordered classes, with `InlineVisibilityCallback` immediately
after `InlineActionClick` and `BlocklistStore` immediately after
`AutoBlockSync`. Patchlet 090 r38 uses these exact ordered gate names:

1. `passive-index-contract`
2. `passive-refresh-contract`
3. `passive-activity-status-contract`
4. `passive-viewport-observer-contract`
5. `passive-visible-scheduler-contract`

Canonical revision-38 code uses one `PASSIVE_ADMISSION_LOCK` for both verified
SQLite generation replacement and the final visible-match admission. The latter
holds that exclusion boundary across the generation/ID-membership check,
passive-running persistence, rate reservation, and `ThreadsBlockBridge`
dispatch. Completed-ID preferences are inspected through raw `getAll()` type
validation, canonical ID checks, and a 5,000-entry bound; malformed or oversized
state returns the automatic path to review instead of fresh capacity. Refresh
failure rearms the full 600,000 ms interval, and forced refresh is direct when
idle or retained only as a bounded viewer-scoped intent behind active work.
Fixed status/runtime codes replace exception text and stack traces.

The rendered visibility callback assembled with apktool 3.0.3 after the
registration-heartbeat change. The complete inline-template tree is bound at
SHA-256
`061b0d6f0cd79719ccc545c06c729e0d8d6dd2eb52df6e4424f6dec7efc4fa24`.
The current canonical autoblock Java tree is bound at SHA-256
`b159f2c0b0d2723a291d01e96ed7672fd008a7991f4e03cde7e6779baabe06ef`.
Those source pins are not signed-candidate or runtime evidence. No pristine
replay, signed-Dex recovery, isolated
Activity probe, device scroll/visibility observation, account action, periodic
refresh timing, or runtime network/database behavior has yet been proven for
revision 38.

### Focused revision-38 static evidence

Focused checks completed on 2026-09-02 without building, signing, publishing, or
installing an APK:

- `Test-Resolution.ps1` passed against the exact source APK and the pristine
  `decompiled/apktool-3.0.3` tree. The observed source SHA-256 was
  `0f6b4515902f78ea194b1bc0563bfcab07f2559d8e80cf6f329d3d1ef786487e`;
  all pinned assets, toolchain inputs, semantic proofs, and pristine rewrite
  states matched. The report is
  `work/passive-resolution-r38-root-c-report.json`.
- The canonical 39-source Java tree compiled to 171 class files and D8 produced
  a 366,680-byte primary DEX with SHA-256
  `5c198b0131b6978c028ece5f3ff3e07195e73b6f1641be3dc5e4995707fea258`
  under `work/passive-java-d8-r38-20260902-a`. Focused JADX recovery of
  `AutoBlockSync`, `BlocklistStore`, and `ModStateStore` satisfied the current
  required, forbidden, count, and ordering assertions. This is a three-class
  source preflight, not the resolution's required 30-class signed-candidate
  recovery.
- `Test-HostPatchletAssets.ps1` passed the offline signed fixture with all 507
  Threads targets indexed, four Facebook entries excluded, and all 507
  normalized usernames unique. Its report is
  `work/passive-final-host-report-20260902-91b63743.json`.
- `Test-PatchletCatalog.ps1` passed nine manifests, the 30-class targeted-JADX
  declaration, exact ownership, release-tool contracts, and the passive
  600,000 ms/index/visibility contract. Its report is
  `work/passive-final-catalog-report-20260902-91b63743.json`.
- The hash-pinned bridge-flow harness passed one positive and all 274 negative
  fixtures. The final inspector SHA-256 is
  `91b63743737d38839c98291c6e22ffa7b8c30999e04a6640e22a7a8d8c4bb1f1`
  and the final fixture-tree SHA-256 is
  `f7011e46c2084904db5156a4d176728be9964bf7eff836d735b78dee95f992a9`.
  The focused report is
  `work/bridge-flow-suite-passive-r38-root-d-report.json`.
- `Test-ReleaseContract.ps1` passed the static release-tool contract, including
  13 proxy-bootstrap, 275 bridge-flow, five direct-string-call, and 20 report-
  permalink fixtures. Its report is
  `work/passive-final-release-report-20260902-91b63743.json`. The report's
  no-artifact `resolution-binding` failure fields belong to the expected
  deterministic-drift negative fixture and are not an attempted APK build.

These focused passes do not promote the resolution beyond `review-required`.
There is still no complete-series replay into a fresh build tree, signed
revision-38 candidate, recovered signed-Dex 30-class proof, isolated Activity
emulator probe for the revision-38 UI, `Test-PatchedApk` result, device scroll
test, account action, periodic timing observation, or live network/database
test. Historical revision-37 artifacts remain ineligible as passive-blocking
evidence.

## Passive blocking revision-40 hardening and focused static evidence (2026-09-03)

At this historical checkpoint, patchlet 020 r16 was the canonical passive
runtime and patchlet 090 r40 was its blocking release contract. This revision retains the exact ten-minute
foreground refresh, indexed one-target selection, native already-blocked
preflight, manual-first shared scheduler, and post/reply row visibility scope,
while closing three fail-closed gaps found during final review:

- every invalid exact-ID lookup now carries the database generation actually
  observed; unknown-generation and current-generation failures latch the
  passive pause, and only a known older result may be discarded after a
  strictly newer verified generation commits;
- raw `list_refresh_not_before` reads catch preference-access failures and
  validate null, type, range, and future bounds as an invalid deadline instead
  of allowing an exception or early retry; and
- the signed target array is all-or-nothing: non-object rows, malformed or
  noncanonical platform fields, malformed Threads IDs or usernames, duplicate
  numeric Threads IDs, and normalized-username conflicts reject the complete
  payload. Only structurally valid canonical non-Threads rows are excluded.

The exact source APK remains
`0f6b4515902f78ea194b1bc0563bfcab07f2559d8e80cf6f329d3d1ef786487e`.
The final `resolution.json` SHA-256 is
`4b54898aac7316a1ab3bb8fd5196878adff125e3a5b937e391601b39bb11f248`
and remains `review-required`. Its canonical autoblock Java-tree binding is
`a84aa72c22a15402968ceb56de9f1da68c107a019ef3e026efb5b071ce8493b2`;
the host Java-fixture tree is
`351867b7ad8aaa8d0713d84e3e8502383c0895ae1d70473f6649f92c3bdfa82d`.
The individual final source hashes are:

- `AutoBlockSync.java`:
  `768f0d73002718d5635c1edf44c6f65fdea0fbaa7c2dba6ecf2eb195fee9a643`;
- `BlocklistStore.java`:
  `64beca51823fa28eae805876e4b1dad9b65964c92e30409efe2ba61c35a07411`;
- `PassiveBlocklistFixtureHarness.java`:
  `b2b322fb98b0caff6dc686adf9c4a37faddd4e7f7d7d46d4be8dbb03e3c64ed7`;
- `Test-HostPatchletAssets.ps1`:
  `4c12df8d6e5f09b894bbfd24e9838682e76590b70851a46ba13d54f64b0234c0`;
- `Test-PatchletCatalog.ps1`:
  `f4d624023e05aa22eca1d554ddbe84d10d0e3e0630bec4b1a9f941e5db64d60e`;
- `Test-ReleaseContract.ps1`:
  `a9504a21f73ea9596ca29f8d679ddd60f7f06421836732423bc66ceb885e09aa`.

Focused checks completed without building, signing, publishing, installing, or
running an APK:

- The complete canonical Java tree compiled from 39 source files to 171 class
  files under `work/passive-source-hardening-java`. The final passive fixture
  harness passed under `work/passive-source-hardening-harness-final`, including
  malformed-row rejection and 507 admitted Threads rows with four valid
  non-Threads rows excluded.
- `Test-Resolution.ps1` matched the exact source APK, pinned assets and
  toolchain, pristine semantic anchors, and six pristine rewrite sets. Its
  final report is `work/passive-resolution-r40-final-b.json`, SHA-256
  `b9b4d9b2e24d3f4c66f3db33cc20a168ae50bf9f4206f0abad4e07c748d48d4e`.
- `Test-HostPatchletAssets.ps1` passed the offline endpoint, signed-list,
  malformed-row, passive-store, limits, reporting, and proxy harnesses. Its
  final report is `work/host-passive-r40-final-report.json`, SHA-256
  `80b7fc64291ebbd3dcc6ede6386b2806df08b301534f36f1e7899323a5e6b2d0`.
  It used no live network.
- `Test-PatchletCatalog.ps1` passed all nine manifests, exact r16/r40 revision
  bindings, ownership, six rewrite sets, the 30-class targeted-JADX contract,
  observed-generation and strict-parser proofs, and retained release-tool
  contracts. Its final report is `work/passive-catalog-r40-final.json`,
  SHA-256
  `52d637feb4a23f86c169eeb51021cf360a0a71f77220668b2b4399b68d29ef65`.
- `Test-ReleaseContract.ps1` passed from a fresh post-edit scratch tree. It
  includes one positive plus 274 negative bridge-flow fixtures, all five
  direct-string fixtures, 13 proxy-bootstrap fixtures, and 20 permalink-flow
  fixtures. Its final report is
  `work/passive-release-contract-r40-final-c.json`, SHA-256
  `624b4cd87b39cf8247a208445ef8d79d4b91405346d94f816b5bf767e071cde3`.
  The report's `resolution-binding` failure fields and `artifactProduced:
  false` belong to the expected deterministic-drift negative fixture; they are
  not an attempted production build.

These checks are source, fixture, and release-tool-contract evidence only.
There is no fresh complete-series replay from the pristine APK, signed r40
candidate, recovered signed-primary-DEX 30-class proof, revision-40 Activity
emulator probe, device installation, live scroll observation, process-restart
timing observation, SQLite fault injection, account mutation, or live mirror
request. Current visibility authority covers post/reply action rows, including
rows in profile post lists; it does not cover profile headers, search results,
or follower/following lists. The historical r37 APK predates passive blocking,
and no APK was produced or published for r40.

## 2026-09-03 signed in-app update resolution evidence (r41 review-required)

Patchlet 020 r17 now owns foreground arbitration between the update controller
and the opening demo fallback. Patchlet `085-in-app-update` r1 owns the six
stable updater descriptors, five `threadsmod_update` preference keys, strict
signed update policy, user-visible prompt, private bounded download, and
installer handoff. Patchlet 090 r41 retains the r40 passive contract and adds
updater host, endpoint, manifest, dialog, installer, and exact
manifest-rewrite gates. The exact resolution also sets
`updateSignedDexReviewRequired=true`: there is no authoritative updater
primary-DEX semantic proof yet, so the signed-APK gate intentionally blocks.

The exact resolution binds the pristine source versionCode/name
`508504469` / `415.0.0.26.77` to clone target versionCode/name
`508504470` / `415.0.0.26.77-threadsmod.1` and target/current mod build 1.
It binds purpose `threadsmod-app-update`, public key
`fYcRAV8CRof15IAinUoDOZuBbqqDtDXDPl3lwSLoMhk`, and metadata reads in this
exact order:

1. `https://raw.githubusercontent.com/nsc55/cloneblocker-mirror/published/threadsmod-update.json`
2. `https://cdn.jsdelivr.net/gh/nsc55/cloneblocker-mirror@published/threadsmod-update.json`
3. `https://h0w1lwun39.execute-api.ap-southeast-1.amazonaws.com/threadsmod-update.json`

The resolution also binds `android.permission.REQUEST_INSTALL_PACKAGES`, the
existing non-exported `com.threadsmod.barcelona.fileprovider`, private
`cache/shared/updates`, and signer certificate SHA-256
`317e3f3813f3b1ec324717faf1bb78f954f49a12122181a85b42338ca10dd079`.
Its required descriptor inventory is 53 classes, including all six updater
classes. Its targeted readable inventory is 36 classes, with the updater six
ordered after `threadsmod.proxy.Socks5VpnService`; each updater entry pins
bounded required, ordered, and forbidden source markers. The legacy field name
`requiredHookCalls` is treated by release tooling as a general final-DEX
required-string inventory and now includes the updater purpose, required-dialog
label, APK MIME type, and install-permission marker.

Strict host evidence covers exact metadata order; GitHub Release and public AWS
object providers; same-provider redirects only; rejection of HTTP,
`tree55.com`, queries, user-info, fragments, IP/private targets, malformed S3
bucket forms, and over-hop redirects; strict JSON and UTF-8; canonical
millisecond UTC timestamps; and RFC 8032 positive/tamper/noncanonical-scalar
signature behavior. Static source gates additionally bind positive typed
rollback floors, newer-revision-only repair after a missing/corrupt envelope,
process-local persistence uncertainty across thrown/false commits until a true
recommit, exact binding between the parsed envelope and typed revision/mod-build
floors, strictly-newer-than-strongest repair for any mismatch, the strongest
attempted policy as a process-local floor, ordinary-load rejection while
uncertain, retained enforcement only for one previously loaded/committed
required policy, boolean-only escape from Store into exactly three load-failure
routes, unavailable/Retry-only UI with no Update/download/unknown-sources or
installer authority, no first-run or optional manufactured lock, exact
package/provider, stale-dialog rejection, fixed one-APK plus one-partial cache
names, and exact archive versionName/versionCode/signers before installer
launch. The executable production-`UpdateStore` harness reports
`PASS update-store-floor false-commit=true thrown-edit=true uncertain-load-rejected=true stale-rejected=true same-repair=true higher-repair=true envelope-below-typed=true envelope-above-typed=true mismatch-equal-rejected=true mismatch-newer-repair=true retained-required=true first-run-no-lock=true optional-no-lock=true`.
That is source-level evidence, not signed-DEX evidence.

The removed provisional updater inspector was marker/count based and could
certify rejected-store paths that still reached persistence, ignored dialog
ownership/showing results, and unconditional verification/installer stubs. It
is not canonical evidence. A replacement must prove CFG dominance and value
flow for verification-before-installer and verified-file provenance,
required/optional dialog controls and cancelability, both eligibility
comparisons, cached-required arbitration before fallback/check, synchronized
`runIfCurrentBinary`, anti-rollback and synchronous atomic commit/uncertainty
floors, and every covering try/catch exceptional successor. It must execute
against both a generated D8 carrier and the final signed APK with positive and
bypass-negative fixtures. Targeted JADX and source appearance cannot clear
this blocker.

Android evidence remains local source, resolution, and fixture policy until
the missing raw-DEX gate and a fresh pristine replay pass with new hash
bindings. Separately, the updater publisher and canonical AWS relay source
`CloneBlockerBackend/deploy/aws-relay/index.mjs` are deployed. The final scoped
VPS archive is `CloneBlockerBackend/work/threadsmod-update-backend-20260903-e.tar.gz`, SHA-256
`4e496d44506bb6e6a46b5a50e9e9d7ef403a8c3b64609f761fd040e010506333`;
all eight updater-owned remote hashes matched and the container was healthy.
The admin publisher derives `minimumModBuild` as a read-only field: clearing
the Required checkbox deterministically publishes `minimumModBuild=0`, so stale
form state cannot silently retain a required-update threshold. The deployed mirror job removes
the exact managed scratch/repository artifacts before each copy so rolled-back
metadata cannot linger. Remote bash syntax and container health passed. The
live Lambda code SHA-256 is
`m28XpDTp/fCsADefHPu9s2IhUcy3qzMW2zv3mnNGMaE=`. Backend tests passed 176/176,
including 23/23 focused updater/relay tests; diff, syntax, and bash checks
passed. Internal probes returned
`/healthz` 200 and `/threadsmod-update.json` 503; public AWS probes returned
update metadata 503 with `{"error":"not_published_yet"}`, blocklist 200, and
admin 404.

No signed metadata manifest, updater-capable APK, or APK object host is
published. This evidence does not prove download, Android unknown-source
consent, installer UI, successful upgrade, restart, rollback resistance on a
device, or any account behavior. The previously published r37 and older APKs
contain no updater and cannot discover mod build 1; a user must manually
install the first updater-capable build once.

## 2026-09-03 patchlet 090 r42 updater raw-DEX review

The replacement updater inspector is now canonical and security-reviewed. `DexUpdateFlowInspector.java` has SHA-256 `f0295aec1f588d578fe92594c71ee1cf17913d2ba9ac9ec0512bff455c322899`. The production-equivalent Java 8/D8 carrier produced one primary `classes.dex`; the inspector covered 24 `threadsmod.update`/`ModBootstrap` root and nested classes, returned all 14 exact semantic checks, and bound normalized updater bytecode to semantic SHA-256 `d89b9170a5188691504087255b6d0b4b6f9a93d930516cec214e3fdb132799bc`.

The generated matrix contains 96 cases: one positive and 95 exact source mutations. Every negative compiled with the same Android 36 library, Java 8 source/target, EdDSA input, D8 tool, and minimum API 28 as the production Java injection. All 95 were rejected by their reviewed semantic code before graph-hash fallback; observed mismatches and hash-only rejections were zero. Coverage includes signature/schema/purpose/key authority, endpoint order and redirect isolation, revision/build/store binding and persistence uncertainty, eligibility, required/optional/unavailable dialog topology, explicit Update authority, retained-policy isolation, bounded verified-file provenance, final current-policy serialization, bootstrap/demo arbitration, and foreground/dialog lifecycle ownership. The harness and signed-APK wrapper each pin literal fixture count 96, inspector argument count 12, and the same ordered 14 check names; patchlet 090 r42 adds AST rejection fixtures for missing, duplicate, nonliteral, divergent, reordered, and wrong-argument expectations.

Security approval permits the exact resolution to become `verified-current` and sets `updateSignedDexReviewRequired=false`. This is resolution and generated-carrier evidence only. No fresh r42 APK has been rebuilt or signed, and no successful r42 release report exists. `Test-PatchedApk.ps1` must compile and run the same inspector against the exact final signed candidate primary DEX and fail before reporting/publication on any graph, class, control-flow, value-flow, exception, argument, signer, archive, or manifest drift. No metadata or APK was published, and no device download, unknown-source consent, installer handoff, upgrade, restart, or rollback test was performed.

## 2026-09-03 patchlet 090 r47 shared bridge-proof pins

The historical 415 resolution now shares r47's fail-closed bridge-tool and
fixture pins, but that maintenance does not make 415 the current release target
or retroactively change any frozen release result above. The intended combined
bridge suite is exactly 315 cases: 279 legacy regression cases plus 36 current
`single-target` cases (one positive and 35 negatives). The current lane covers
the authority quartet, shared-lock ordering, provisional-budget cleanup,
retained/local `List` rejection, backward same-owner selection-edge rejection,
and current started, failure, success, watchdog, and fresh-drain bypasses. The
legacy positive remains fixed at automatic reviewed terminal routes / terminal
catch routes / paced-next posts = 5 / 1 / 1. The
current positive and every current production candidate must instead report
`ownerMode: single-target` and 3 / 1 / 0. Historical 272- and 275-case results
remain facts only about the exact earlier inputs that recorded them.

The focused generated-Smali combined harness report
`work/r47-authority-suite-final315-a-report.json`,
SHA-256 `3781b4e9259906eaff511ddcb706a826156670e0823fa139207e79a140fbfde7`,
records `status: passed` for all 315 fixtures. It is current r47 fixture evidence,
not 415 signed-candidate evidence and not a complete 444 `SignedReview` or release.

The accompanying r47 release-tool AST graph is required to contain exactly six
callers, 22 contracts, and 26 invocations. That is a current contract
requirement, not passed 415 or 444 evidence. The first 444 `SignedReview`
stopped at `release-gates` on the stale `automatic_is_current_owner_shape`
verifier. Its build report records `artifactProduced: false`; there is no
`release-check.json`, publication move, or `dist` output. The work-only APK is
135,133,736 bytes at SHA-256
`3c1e0c86a8c76f682f1aaf232ff7422c560a64d34899c250cf3836199f6d28d3`,
and its 11,623,852-byte primary `classes.dex` is SHA-256
`9ccf7441e567405f947037267e5fc1c4e779e2e2c3f369a9d3edd9ccfe677caa`.
Its content-identical reapplication covered 124,320 files, 8,163 directories,
and 132,483 entries at tree SHA-256
`ea55b8175e731b9a2ee2bf3e8d66d7959fbf10994eac96d28db9e3f5721294c4`.
Those bytes are failure evidence only. The r47 source correction still requires
a fresh 444 signed review, human promotion, and independent normal `Release`
replay.
