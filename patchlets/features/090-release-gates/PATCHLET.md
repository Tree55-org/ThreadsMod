# 090 — Fail-closed release gates

Revision 62 (2026-09-06) binds the 020 r24 chunked v3 client. `Test-HostPatchletAssets.ps1` now compiles and runs the harnesses against the v3 fixture tree `assets/tests/fixtures/blocklist-v3-2026-09-06` (bound as `assets.signedBlocklistV3FixtureTreeSha256`; the legacy `blocklist-signed-2026-08-30.json` and its binding are removed), pins the three `blocklist/v3/manifest.json` read URLs and the three `objects/` bases exactly once each in declared order, forbids the retired whole-file URL tails in every production asset, raises the `https://` literal count from 10 to 13, proves that `CloneBlockerEndpoints.objectUrl` validates the object name before any concatenation, replaces the thirteen whole-file target-loop anchors with an ordered signed-root chain (byte cap, envelope, signature, timestamps, `v` 3, `sha256-hi32`, chunk caps, threads partition, bucket/row caps, `VerifiedList`) and an ordered chunk-row chain in `ChunkInstaller.parseRow` (object shape, `i`/`u` reads, `END_DOCUMENT`, id grammar, username, bucket membership, duplicate id, duplicate normalized username, `Entry`), pins the seven fixed whole-chunk row-rejection literals together with the gzip, inflated-byte-cap and group-table literals, the `bucketOf` body and the two bucket-key prefixes, requires `ObjectFetcher.fetchOnce` to build the URL before `openConnection()`, send `Accept-Encoding: identity`, bound Content-Length and streamed bytes, and prove SHA-256 before `return bytes;`, forbids `JsonReader`/`GZIPInputStream`/`BlocklistStore` in the fetcher and `HttpsURLConnection`/`setLenient`/`PASSIVE_ADMISSION_LOCK`/`replaceVerified(` in the installer, adds both classes to the raw-Throwable flow loop, and re-pins the store: `DATABASE_VERSION = 3`, the UNIQUE username index, the `h32` column and index, the chunk/group/staging DDL, the widened `target_count` bound, `bucket_bits`, the `InstallPlan` signature of `replaceVerified`, the `NOT EXISTS` set-difference query of `countNewTargets(SQLiteDatabase, String, int)`, the ordered proof that each bucket's set difference is measured before its range delete, the range-delete and `INSERT ... SELECT` literals, the `coalesce(sum(id_count), 0)` total, the chained `if (newVersion != 3 || oldVersion < 1 || oldVersion > 2)` migration with its `DROP INDEX`, two `RENAME TO`, `copyRetainedTargets`, two `DELETE FROM ..._v2` and preserved-metadata invariants, and the rule that `snapshot` and `markFetchedUnchanged` no longer call `requireRowCount`. The fixture harness line is now `PASS passive-fixture-v3 k=4 chunks=16 idRows=1607 handleRows=26 unique=1607`; the report's `passiveBlocking` block reports `schemaVersion` 3, the bucket function and the chunk tables. `Test-PatchletCatalog.ps1` requires 020 r24 and 090 r62, the v3 read endpoints, the fourteen-entry v3 `databaseSchemas` list (including the two retired empty v2 tables), `Lthreadsmod/autoblock/ObjectFetcher;` and `Lthreadsmod/autoblock/ChunkInstaller;` owned by 020 among 54 required descriptors, targeted-JADX contracts for both new classes (fixed rejection literals, `stageChunk`/`pruneStaging`/`stagedShas`/`readChunkTable`/`fetchObject` calls, `Accept-Encoding`/`identity`/`MessageDigest`, and the diagnostic, fetch/parse-separation and lock forbidden strings), the new BlocklistStore required strings and counts (`CREATE UNIQUE INDEX blocklist_targets_username_idx` and `stageChunk(` once each, the retained `CREATE INDEX blocklist_targets_username_idx` still once through the kept v2 constant), the source-level root chain, chunk-row chain and single-occurrence rejection literals, and drops the two legacy parser aliases; the seven legacy row-rejection literals leave the AutoBlockSync contract in the resolution and `Test-ReleaseContract.ps1`, replaced there by the eight signed-root literals, `ChunkInstaller.stage(` and `signed objects could not be staged` (which `Test-PatchedApk.ps1` enforces through the resolution's `requiredStrings`; its own AutoBlockSync minimum list carries no row literal), and both signed-DEX gates carry the new class maps and count pins. The resolution's `requiredReadEndpoints` and the schema's three consts name the v3 manifest URLs; the CloneBlockerEndpoints contract requires the three object bases and the allowlist literal and forbids the retired URL tails class-scoped. The 48 bridge-flow negatives were rebased onto the regenerated carrier (`.line` delta 41, accessor renumbering) and `dexBridgeFlowFixtureTreeSha256` re-pinned; the inspector sources are unchanged and the matrices stay at 328, 41, 13 and 96. The reviewed series is 010 r5, 020 r24, 050 r17, 060 r14, 070 r10, 085 r2 and 090 r62; only 020 and 090 moved from the revision 61 series published earlier on 2026-09-06 (020 r23, 090 r61). Revision 62 was reviewed by the r65 `SignedReview` (candidate SHA-256 `4a2af379085f3c8b55676067c876e8b82143f267cf198c12f0f18fb9780dd1b2`, 135,150,120 bytes, completed `2026-09-06T18:44:17.0961996+07:00`, `runtimeValidation: isolated-activity-ui-probe-passed`, `activity-ui-review` SHA-256 `d49de81908b2bb951ca1eed3d13382689375a83080b181a2ff65d4127bb0582e`; review records `build-report` `84fc477462b8ca603090748534b43d99c8be6fe5fd1f1e447873b6bbf40f92ed`, `release-check` `90a3a15f5002095adc868faf9f071f6491fe6557b52c212201b2025d1b6e4cb7`, `idempotency-check` `537040e9987d8a22738e9c5296258f1b3aed68158248bcfb6bafb9cb52e8a8b3`, `catalog-check` `8b5aa0c4170d2d0dfc6be4cbaa5023a231421930a35d85bc9ff5efa0e43397df`, `resolution-check` `aeb67ce72caf94b92b589ae03c4bfa99d79d6e4505e8fd8670805823084a7558`; review-time resolution SHA-256 `09935bc711e078124b7a45051610bcf02e32caa1e53fd6edbb99a684c5fac676` and frozen canonical tree SHA-256 `5889b35ff05f66f5cf18d5e99b58a842e61bbbe3f624902cbda269d4fb4590f0`), promoted at `2026-09-06T18:45:02+07:00` (resolution SHA-256 `9b96cce9134d207264ed42b9ecd4e92d10f3c630c58d9908cff755b00eeb9371`, changing only `status` to `verified-current`, `resolvedAt`, and `release.updateSignedDexReviewRequired` to `false`), and published (released APK `dist/ThreadsMod-CloneBlocker-444.0.0.45.85-arm64-v8a-threads55-mod1-d.apk`, 135,150,120 bytes, SHA-256 `e6ec4d70dacfa094659e31e09f2a890c537ba72b574767d62337a6a929c672a4`, completed `2026-09-06T19:28:00.0944447+07:00` from split set `5c1f06bdda17d5b58677d8941b0264c51135344cc257d93818e6f19d63504bff` and universal source `156ae30cef15a2de7ce9bec2637a68a51f0ab2547ae7b7e7ff9541853d9670ec` against frozen canonical tree SHA-256 `0665a733a29b60f195c4f9f53f28462fd938d4cefded6a535d623d4556c83cfc`; release records `build-report` `3f22ee05cbd61cf8d76c03b0653289c5b655f2a07afe16cb7bb81aeac6e92c13`, `release-check` `0f3fab6208190df119c1290a904fe1d6107b29e006c5a955c6145e1fb7bdf54f`, `idempotency-check` `5c49e7af678f2b352a832b4aa6a15f26de5ca5269e261dfce83d7ae9964582a3`, `catalog-check` `d16bfde599c1e3e7f54496eb50bd210f0b33ecbf63a2271742e41a9636703975`, `resolution-check` `5e0df81015116c66e9cc8fdfaaac82ba6da0251bbe978342caa92261b598c5c6`; `status: passed`, `validationMode: Release`, `releaseEligible: true`, `artifactProduced: true`, `runtimeValidation: not-run`; freeze checks matched with zero tree-monitor events; decoded tree SHA-256 `d6e9c44afb84fc053c29a6b0d652fbce0edc1fd67ecaa45c97b83eb434d20057` before and after across 124,329 files; primary DEX 61,821 method references; one v2 signer, certificate SHA-256 `317e3f3813f3b1ec324717faf1bb78f954f49a12122181a85b42338ca10dd079`; 16 KiB alignment passed) on 2026-09-06; both runs passed the 328, 41, 13 and 96 matrices, the update-flow semantic SHA-256 stayed `97b8947b803ef4dcdecefd315507bc1e5f9c27f89ab402639ab2646b46bde9f2`, and the candidate and published `classes.dex` (one and the same SHA-256 `a91c942c51de51e8ff754c2a4c9d8c3f22600531c459942eed03430e0857c9b8`) were each checked directly: `blocklist/v3/manifest.json` and `blocklist/v3/objects/` are present six times each, `Lthreadsmod/autoblock/ChunkInstaller;`, `Lthreadsmod/autoblock/ObjectFetcher;`, `CREATE UNIQUE INDEX blocklist_targets_username_idx` and `signed root is not v3` are present, and `published/blocklist.json`, `@published/blocklist.json`, `amazonaws.com/blocklist.json`, `Threads Mod Auto Block`, `tree55.com` and `MAX_TOTAL_` are absent. The published bytes have not been installed or run on any device; no v3 refresh, migration or chunk download has been exercised on a device, the only device evidence is the r65 candidate's isolated Activity probe on an emulator, and every gate above is static proof, not device-runtime proof.

Revision 61 binds the 020 r23 list-refresh repair. `Test-HostPatchletAssets.ps1` keeps every existing cadence pin (the `FETCH_INTERVAL_MS`, `MAX_LIST_REFRESH_DEADLINE_FUTURE_MS` and `KEY_LIST_REFRESH_NOT_BEFORE` literals, the three `armListRefreshWake` wait branches, the worker-completion order advance -> release -> post -> `millisUntilNextListRefresh(active)`, and the `readListRefreshDeadline` single-`prefs(context)` slice), re-pins the worker's completion advance as `advanceListRefreshDeadline(activity.getApplicationContext(),System.currentTimeMillis(),nextIntervalMs)`, and adds a bounded-recovery block that requires `MAX_RESPONSE_BYTES = 4 * 1024 * 1024`, the fixed `LIST_REFRESH_FAILURE_LADDER_MS = listRefreshFailureLadderMs()` declaration, the helper's five explicit stores (`long[] ladder = new long[5]; ladder[0] = 15L * 1000L;` through `ladder[4] = 300L * 1000L;`, built element by element because an array literal compiles to a `fill-array-data` payload in the static initializer that `DexBridgeFlowInspector` refuses as `value_flow_control_flow_unsupported`) and the `MAX_LIST_REFRESH_FAILURE_RETRIES = LIST_REFRESH_FAILURE_LADDER_MS.length` five-step bound, the success/failure branch that selects `nextIntervalMs`, the `intervalMs > FETCH_INTERVAL_MS` guard and the 600,000 ms two-argument wrapper, both `MAX_RESPONSE_BYTES` enforcement sites, the transport-only `isTransportFailure`/`throw firstError` retry with exactly two `fetchMirrorOnce(` calls inside `fetchMirror`, the per-mirror `classifyListFetchFailure`/`describeMirrorFailures` status, and the `consumeMissingIndexResumeStart` admission bypass. The reviewed-diagnostics regexes are unchanged and still forbid any message, `toString`, or logging flow of a caught Throwable. The resolution's targeted-JADX count for `advanceListRefreshDeadline(` moves from 4 to 6 (two definitions, the wrapper call, two admission calls, one worker call) and `Test-ReleaseContract.ps1` mirrors it; `KEY_LIST_REFRESH_NOT_BEFORE`, `LIST_REFRESH_DEADLINE_LOCK` and `readListRefreshDeadline(` stay at 3. The bridge-flow negative fixtures were rebased after the line shift in `AutoBlockSync.java` (all fourteen pinned `.line` numbers move by one uniform delta, +137 against the r61 carrier, and the synthetic accessor names renumber); the bridge-flow matrix stays at 328 and no other fixture matrix moves. The revision-38 full-interval failure rearm and revision-39 remaining-delay proofs are superseded for failed attempts only, as revision 54 superseded revision 38's capacity portions. This half of revision 61 was reviewed by the r64 `SignedReview` (candidate SHA-256 `888130a25938b9c7fe06ecb0e0e62056839e322f7498b317c5f0a20ce20861a5`, `runtimeValidation: isolated-activity-ui-probe-passed`, `activity-ui-review` SHA-256 `6a39465b817e96056bfc077f1d2fbd9e28a2f3e7175bdfb979a6c4a3ec17c224`; review records `build-report` `fae2d43f044a8f592082970dba875b13c4cb2f04812548ffec67c8eab545762b`, `release-check` `f05434e3e671a4292b123cc629e51566986b620af00f1ef8c640eefa2053bfbb`, `idempotency-check` `1bc882ae7f0a737568903c548c2bbcbb783e39b5c7d5fb63bf41de76fe17005b`, `catalog-check` `26688f042629ba9fb14f8c236a2d5a9c5fc6b0957b29335884440235d94b4cd0`, `resolution-check` `aaefc029012287427f271ccb7aca254874df75c4a453627fbee416a1651e0bbe`), promoted (resolution SHA-256 `6a34bfa8ce869ee36bef4e57805cf8a03850839028412fd425eda77444526193`), and published (released APK `dist/ThreadsMod-CloneBlocker-444.0.0.45.85-arm64-v8a-threads55-mod1-c.apk`, SHA-256 `da17a14814b73ca4e94c02fe9f8e7382a0747a07682a84002b6aa086e90ad4fb`) on 2026-09-06; both runs passed the 328, 41, 13 and 96 matrices, and the candidate and published DEX were each checked directly: the closed failure token `too_large` is present.

Revision 61 binds the 020 r23 removal of the first-run notice and the 085 r2 continuation contract. `proveBootstrap` in `DexUpdateFlowInspector.java` no longer proves a `DemoDialog.showOnce` caller; it proves the continuation's constructor takes no arguments, that its `run()` is a single `return-void` (`bootstrap_fallback_not_empty`), and that the dialog class is absent from the DEX (`bootstrap_demo_dialog_present`), while the arbitration-first, dominance and three-caller checks are unchanged. The resolution's targeted-JADX entry for `ModBootstrap` drops the dialog call from its required and ordered strings and forbids the `DemoDialog` token; the required class descriptor list drops the dialog, 53 to 52. The three demo-dialog negatives (`bootstrap-demo-before-update-arbitration`, `bootstrap-demo-runnable-not-passed`, `bootstrap-duplicate-demo-fallback`) are replaced one for one by `bootstrap-continuation-not-empty`, `bootstrap-continuation-captures-activity` and `bootstrap-demo-dialog-class-reintroduced`, so the updater matrix stays at 96. The update-flow semantic hash is rebound to the regenerated positive, `97b8947b803ef4dcdecefd315507bc1e5f9c27f89ab402639ab2646b46bde9f2` (previously `d0e8d1ab489ab6241779cb18c21621f3ebd7f17310b36a18d49d6081d7d4d1ed`), which the exact resolution pins as `expectedSemanticSha256`. Revision 61 was reviewed by the r64 `SignedReview` (`work/patchlet-444-r64-signed-review-20260906-a`, candidate SHA-256 `888130a25938b9c7fe06ecb0e0e62056839e322f7498b317c5f0a20ce20861a5`, 135,133,736 bytes, completed `2026-09-06T15:13:09.2192012+07:00` under review-time resolution SHA-256 `65146501defd019fa813666b87fd1d9d4e1927682423909978aa08cf657a5089`; its signed-candidate update-flow inspection and its 96 generated-D8 updater fixtures both bound `97b8947b803ef4dcdecefd315507bc1e5f9c27f89ab402639ab2646b46bde9f2`, and its primary DEX contains neither `Threads Mod Auto Block` nor `Lcom/threadsmod/DemoDialog;` nor `Disabled until you explicitly enable it` and does contain `too_large` and `Enabled; waiting for a signed-in foreground session`); promoted at `2026-09-06T15:13:52+07:00` (resolution SHA-256 `6a34bfa8ce869ee36bef4e57805cf8a03850839028412fd425eda77444526193`, changing only `status` to `verified-current`, `resolvedAt`, and `release.updateSignedDexReviewRequired` to `false`, which the schema had re-armed when the resolution returned to `review-required`); and published (fresh default `Release` `work/patchlet-444-r64-release-20260906-a`, completed `2026-09-06T16:02:13.4195710+07:00`, replayed from split-set SHA-256 `5c1f06bdda17d5b58677d8941b0264c51135344cc257d93818e6f19d63504bff` and universal source SHA-256 `156ae30cef15a2de7ce9bec2637a68a51f0ab2547ae7b7e7ff9541853d9670ec` against that promoted resolution with frozen canonical `patchlets/**` tree SHA-256 `0b9123df0e297a194c0193183040b2d33fced155f1fafe9e0e81366d527c875d`; released APK `dist/ThreadsMod-CloneBlocker-444.0.0.45.85-arm64-v8a-threads55-mod1-c.apk`, 135,133,736 bytes, SHA-256 `da17a14814b73ca4e94c02fe9f8e7382a0747a07682a84002b6aa086e90ad4fb`; release records `build-report` `2e6530c642de91d0255c9abd361e8ed92c4d86845e8e93daaa510fc81b15ee5a`, `release-check` `dc914aa4ab08106d1c623ec3d58e3b7e70d9f09e15eb6e51853fb599afaa247f`, `idempotency-check` `97b7a93269408c742fcad500a59725e552e1c373f01d466beae95ed774e50d53`, `catalog-check` `50f854e525a58c7eb8d91d8d944e98fc8ddec2bdda2c2c5a646f068621cd80f3`, `resolution-check` `5777bb82788e97de8cef068112d0e4dfc720e086d9328b81ae447f992d6f0dda`; `status: passed`, `validationMode: Release`, `releaseEligible: true`, `artifactProduced: true`, `runtimeValidation: not-run`; reapplication content-identical across 124,321 files, 8,163 directories and 132,484 entries with decoded-tree SHA-256 `8d47fa9d2145beb92a550ee816a1df861af7916d1d6b64a099090a9f56c242ee` before and after; primary DEX 61,746 method references; exactly one v2 signer, certificate SHA-256 `317e3f3813f3b1ec324717faf1bb78f954f49a12122181a85b42338ca10dd079`; 16 KiB alignment passed; pre-report, pre-publish and post-publish freeze checks matched with zero tree-monitor events; the published DEX was checked directly with the same five results) on 2026-09-06. The published bytes have not been installed or run on any device; the only device evidence is the r64 candidate's isolated Activity probe on an emulator, and neither the static gates nor that probe exercise the clone's own launch, a list refresh, Block, report, ping, update or VPN behaviour.

Revision 60 binds the runtime-changing 020 r22 and 050 r17 corrections. No gate expectation moved: neither corrected sentence was pinned, the host gate's always-on assertion on `isEnabled` is unchanged, and the bridge-flow negatives were rebased onto the regenerated carrier after the one-line net shift in `AutoBlockSync.java`. The matrices stay at 328, 41, 13 and 96. Revision 60 was reviewed by the r61 `SignedReview` (candidate SHA-256 `7252940817521e93455b040d355bbd8e35951cb917a6b36bb16bf5d9f45fe23a`, `runtimeValidation: isolated-activity-ui-probe-passed`), promoted (resolution SHA-256 `04e92a63eedd051b6c471607d929ab9737290b7664b0a13dbfa06d8653bfc9e5`), and published (released APK SHA-256 `abcfb52fc23b1d819488789566c62189b5297183bf5e39652f17f132272d3bf8`) on 2026-09-06; both runs passed the 328, 41, 13 and 96 matrices and recovered all 38 targeted-JADX classes, and the published DEX was checked directly: `Disabled until you explicitly enable it`, which no gate pins, is absent and the always-on fallback is present.

Revision 59 binds the runtime-changing 010 r5, 020 r21 and 050 r16 changes. The host asset gate no longer requires an `AutoBlockSync.disable(Context)` block and instead requires `isEnabled` to be exactly the always-on form: it must contain `return context != null;` and must not read a preference, which is what proves passive blocking cannot be turned off in a shipped build. The exact Settings passive disclosure pinned by the catalog gate, this contract, `Test-PatchedApk`, and the resolution's `requiredHookCalls` all moved to the `Always on.` wording together, so a build whose UI text and whose final DEX disagree still fails closed. The isolated Activity UI probe requires `Sync now` on the Settings screen. The 48 bridge-flow negative fixtures were rebased onto the regenerated carrier after the `AutoBlockSync.java` edits shifted every D8 `.line` anchor by a uniform -17 and renumbered the synthetic accessors; the matrix stays at 328 cases, and the updater and report-permalink matrices are unchanged at 96 and 41. Revision 59 was reviewed by the r60 `SignedReview` (candidate SHA-256 `f7a5ee38c07d050e34defa73d1c6737e8c340f7f7187327d6d13cafe40488f9e`), promoted (resolution SHA-256 `6014c9090e86c8ba7c8497addc47936ee8f9f73aa673e58f4f24c3b582d1f42b`), and published (released APK SHA-256 `ca53165cc8f511e17b5967b49aae0c4b0fb0629da7da6dd0c46718250f1b3e73`) on 2026-09-05. That APK, `dist/ThreadsMod-CloneBlocker-444.0.0.45.85-arm64-v8a-threads55-mod1.apk`, is superseded: its `getStatus` fallback reported `Disabled until you explicitly enable it.` on a fresh install while passive blocking was running, a user-visible text defect that no gate pinned and that the documentation verifier sweep found after publication, so it should not be installed; revision 60 and the 2026-09-06 release replace it.

Revision 58 binds the runtime-changing 010 r4 and 070 r10 changes and rebinds
the updater proof. The exact 444 resolution's identity values are now
`app.tree55.threads`: `applicationId`, `requiredProviderAuthorities`,
`requiredTaskAffinities`, `requiredCustomPermissions`, and
`requiredAuthenticatorAccountType` are checked in the signed APK, the
canonical `UpdateManifest.APPLICATION_ID` and updater FileProvider constants
carry the same identity, and the resolution schema pins `fileProviderAuthority`
to the constant `app.tree55.threads.fileprovider`. The resolution's
`requiredStatisticsEndpoint` is the schema constant
`https://h0w1lwun39.execute-api.ap-southeast-1.amazonaws.com/v1/installs`,
proven beside the unchanged `/v1/reports` `requiredWriteEndpoint` as the second
and final reviewed POST destination; the host-asset gate requires the
two-write-path contract (`reads=3`, `write=` the `/v1/reports` URL, `stats=` the
`/v1/installs` URL) and runs the `InstallStatsHarness` over the closed 13-key
`InstallStatsPayload`, whose fixed key order includes the constant
schema-version key `v` and whose `safeToken` charset admits the forward slash so
an IANA time zone survives intact. The updater lane's raw primary-DEX
update-flow semantic hash is rebound to
`d0e8d1ab489ab6241779cb18c21621f3ebd7f17310b36a18d49d6081d7d4d1ed` for the r59
review; both the r59 SignedReview and the 2026-09-05 Release record that hash
in their passing 96-case `dexUpdateFlowFixtures` result. Two AGENTS.md wording
mismatches found by the independent four-reviewer workflow were reconciled by
the promoter before the Release: the closed field set now names the constant
schema-version key `v` as its 13th member, and the recovered
`report_request_unavailable` literal count is stated as 2.

`SignedReview` r59 at `work/patchlet-444-r59-signed-review-20260904-a` then passed the complete frozen review under review-time resolution SHA-256 `d475da9d2688be4924899bcd11e7e1916f97eb6d0f38724e4a5ec2884941a8d8` and produced exact work-local candidate SHA-256 `280fa71d00315dbadcece0de5b3fcf19dffd542ee95a8052f979c2dbe86cf9b8`, 135,133,736 bytes, with `runtimeValidation: isolated-activity-ui-probe-passed` and `completedAt` `2026-09-05T00:23:06.7721632+07:00`; its `build-report.json`, `release-check.json`, `idempotency-check.json`, `catalog-check.json`, `resolution-check.json`, and `activity-ui-review.json` have SHA-256 `6f97a9b1590eb1ace739dfc1ec7bbced3fd427af6bc49911b4e2bfa8c2d5c5e2`, `60d861a1b400163e2c68248c3bcfac74fccf4a2f34343e925b3e10b33f52aae1`, `e9d340b290f070b943b18a4aa8229885473762e1642e6aa21ae9d5684ff2de3d`, `7742eb897409541f5bdd6b6904ce6c657dd1023888c2a213ed036bfd5d1c989e`, `59cb133c033f19755f1c7cc464a95e3972dd52a25470552c65e826d172119941`, and `2053c1fde7d41c5b98a67023be522e804f2545d5f4e4c3b2f3c147e118694cad`. Explicit human approval of that exact candidate promoted the Threads 444 resolution to `verified-current` at `2026-09-05T10:48:38+07:00`, resolution SHA-256 `7ac5954964677708c827057faf98e13da40b06adb68dba110c72c13185954784`, with `updateSignedDexReviewRequired=false`; only `status`, `resolvedAt`, and that flag changed, and the independent four-reviewer workflow found no blocker. The fresh default `Release` at `work/patchlet-444-r59-release-20260905-b` then passed and transactionally published `dist/ThreadsMod-CloneBlocker-444.0.0.45.85-arm64-v8a-tree55-mod1.apk`, 135,133,736 bytes, SHA-256 `7a8850ef96fe1f973358b1b07989090e3f94ec9ce0a30addb38e304ea8d3a1e6`: package `app.tree55.threads`, `versionCode 511407878`, `versionName 444.0.0.45.85-threadsmod.1`, minimum/target SDK 28/36, arm64-v8a, mod build 1, one signer certificate SHA-256 `317e3f3813f3b1ec324717faf1bb78f954f49a12122181a85b42338ca10dd079`, replayed from split-set SHA-256 `5c1f06bdda17d5b58677d8941b0264c51135344cc257d93818e6f19d63504bff` and universal source SHA-256 `156ae30cef15a2de7ce9bec2637a68a51f0ab2547ae7b7e7ff9541853d9670ec` under frozen canonical `patchlets/**` tree SHA-256 `b3cb3bd58366f32edcb35cb9646281ef93b0e020d7a3259ba13e21ea631c8cd7`. Reapplication was content-identical across 124,325 files, 8,163 directories, and 132,488 entries with decoded-tree SHA-256 `73545eeafc37f3dd25c0a6e622a760c31917ca3deacf52d4ede6e7ddaaa97300` before and after; primary DEX has 61,744 method references; the fixture matrices passed at 328 bridge, 41 report-permalink, 13 proxy-bootstrap, and 96 updater cases with all 38 targeted-JADX classes recovered. Its `build-report.json`, `release-check.json`, `idempotency-check.json`, `catalog-check.json`, and `resolution-check.json` have SHA-256 `4e3a5a00f2839004a5d215b443f4cd3a72294855d61011094ce455da49db287f`, `703d17e39e1dbea107191939f5c57b59d82eb08bb724a0ef7cd692c7819407d4`, `738e3d674bdd6d11507d9f6d602072808e9073c96068b69d2f96615323a010cf`, `14254876d43d28ffacdce4ac58a72d64d279a9de2007fe2ed4fa1f5ad270a8b5`, and `495cc0c48bab0b8b3f56a0e2a16c87b5a463f7e2982854b9fe704e1c270fb31b`; `completedAt` is `2026-09-05T12:31:28.9580718+07:00` with `validationMode: Release`, `reviewOnly: false`, `releaseEligible: true`, `artifactProduced: true`, and `runtimeValidation: not-run`. Installation and runtime validation remain separate.

The earlier Release attempt at `work/patchlet-444-r59-release-20260905-a` is an aborted external interruption: the host terminated it during the smali rebuild because the machine ran low on memory while a headless emulator was resident. It wrote no `build-report.json`, published nothing, left no `.publishing-*.tmp` residue in `dist/`, and did not alter the resolution, whose SHA-256 was re-verified afterwards as `7ac5954964677708c827057faf98e13da40b06adb68dba110c72c13185954784`; the directory is retained as an aborted run and its name cannot be reused. The published APK has not been installed on any device: the r59 SignedReview candidate, not this artifact, launched to the login screen on an arm64-capable emulator without a crash, and two `adb install` attempts of the published artifact onto the Xiaomi 2310FPCA4G test phone were cancelled by the device's USB-install confirmation (`INSTALL_FAILED_USER_RESTRICTED`). No runtime fact exists for the published bytes; login, scrolling, Block, report, ping delivery, update, and VPN behaviour were not exercised on a device. The r53 Release-b artifact `dist/ThreadsMod-CloneBlocker-444.0.0.45.85-arm64-v8a-update-mod1.apk` (SHA-256 `85124df5d5995af7ca3efd80b1a16f14b0e8fd380fc61e1bfe303b4037708ec4`, package `com.threadsmod.barcelona`) is a historical fact that was removed from `dist/` on 2026-09-06 together with the `tree55-mod1` and superseded `threads55-mod1` artifacts, after the documentation append that first described the `-b` build (the `dist/` directory's modification time is `2026-09-06T12:30:14+07:00`); byte-identical copies survive only as the run-local Release outputs `work/patchlet-444-r60-release-20260905-a/release/ThreadsMod-CloneBlocker-444.0.0.45.85-arm64-v8a-update-mod1.apk` (SHA-256 `ca53165cc8f511e17b5967b49aae0c4b0fb0629da7da6dd0c46718250f1b3e73`, the superseded `threads55-mod1` build), `work/patchlet-444-r59-release-20260905-b/release/ThreadsMod-CloneBlocker-444.0.0.45.85-arm64-v8a-update-mod1.apk` (SHA-256 `7a8850ef96fe1f973358b1b07989090e3f94ec9ce0a30addb38e304ea8d3a1e6`, the `tree55-mod1` build), and `work/patchlet-444-release-20260904-b/release/ThreadsMod-CloneBlocker-444.0.0.45.85-arm64-v8a-update-mod1.apk` (SHA-256 `85124df5d5995af7ca3efd80b1a16f14b0e8fd380fc61e1bfe303b4037708ec4`, the r53 `update-mod1` build), each 135,133,736 bytes, verified with `sha256sum` on 2026-09-06, and `dist/SHA256SUMS.txt` is unchanged and still lists all four 444 artifacts; its three lines for the removed files verify only against those run-local copies; the new package installs beside it, the updater cannot bridge the two package names, and `adb install -r` cannot update across them.

Revision 57 binds the runtime-changing 020 r19 and 060 r14 corrections. It
requires the 444 row adapter's `const v12, 0xf700` native default mask
(modifier bit clear, icon-size bit set) in the canonical template, the positive
fixture, the resolution, the schema constant, the signed-DEX report-permalink
inspector, and every wrapper; the report-permalink matrix is now 41 cases,
adding the `row-ufi-zero-icon-size-mask` bypass negative beside the rebased
modifier-substitution negative. The host-asset gate proves the freshest-verified
refresh shape: `fetchMirror` returns a verified candidate and installs nothing,
`refreshBlocklist` walks the exact mirror array from index zero, keeps only the
strictly newest verified candidate, and calls `installVerifiedCandidate` exactly
once after every mirror was consulted, and the replace/conditional-metadata
ordering is proven inside that install method. It adds `already_completed` /
`CB-SCH-107` to the closed diagnostic vocabulary and to the BlockDiagnostic
targeted-JADX contract. Revision 57 was canonical source and static evidence
only at its checkpoint: no complete SignedReview, promotion, Release, or
publication existed for r57 bytes, and the r59 SignedReview, promotion, and
Release recorded above belong to the r58 series.

Revision 56 changes release evidence only; it does not change runtime Java,
rendered Smali, host rewrites, or APK behavior. Revision 55's
`SignedReview-a` (`work/patchlet-444-r55-signed-review-20260904-a`)
produced signed work-local candidate SHA-256
`8678f65c9b7c5368f4a1711117748f6f87be99190a0bdf14f42880ec3ab069a2` and
passed `release-check.json` SHA-256
`64b1403cfb5dde825fec96037a88818050f460d5c2de5d4818daa340b034af48`, then
failed at `signed-review-activity-ui`; its `build-report.json` SHA-256 is
`1a3baef9d67339e3092594ca5f11d5b249fddddf126f509bd318c152addfc49a`.
The status existed as one visible multiline node, but the old probe searched
for each dynamic line as an exact raw XML attribute and therefore produced a
false negative. The build report records `artifactProduced: false`, verified
publication rollback, and runtime `not-run`; r55 remains failure-only evidence.

Revision 56 replaces only that stale matcher. It parses bounded DTD-free UI
XML with external resolution disabled, derives a nonzero root viewport,
requires nonzero matched-node bounds that intersect the viewport, matches
fixed labels by exact visible node text, and accepts the five dynamic status
fields only as one contiguous ordered multiline sequence whose values are all
nonempty. One positive fixture and nine bypass negatives cover
content-description-only, mid-line prefix, wrong prefix order, missing prefix,
malformed XML, zero bounds, offscreen bounds, empty status value, and split
status nodes. The focused corrected probe report
`work/r56-activity-probe-focused-b.json`,
SHA-256 `c1d7bc800140c64c489e675b6b193a195da222d6699f37e56e0aeade5da9bdc7`,
passed all three probe Activities, exact primary-DEX equality, and the 1/9
matcher fixtures. It is focused tool evidence only, not a complete r56
`SignedReview`; the fresh canonical replay it required was later supplied by
the r59 SignedReview recorded under revision 58.

All other current release expectations remain unchanged, including the exact
328-case signed bridge matrix: 279 legacy fixtures plus 49 current
`single-target` fixtures, one positive and 48 negatives. Revision 56 does not
allow the Activity matcher correction to rebind or waive those contracts.

Revision 55 changes release evidence only; it does not change runtime Java, rendered Smali, host rewrites, or APK behavior. The first r54 SignedReview reached the signed candidate and all raw-Dex fixture lanes, then failed closed because its resolution expected four readable `AutoBlockSync.recordInlineRenderStage(` expressions while pinned JADX 1.5.6 deterministically duplicated the shared `report_request_unavailable` block and recovered five. The exact adapter still contains four raw invokes and one copy of each fixed diagnostic literal. Revision 55 binds the recovered count to five, keeps the raw/template count at four, and documents that decompiler duplication is secondary readable evidence rather than callsite authority. A fresh complete SignedReview remained required at that checkpoint and was later supplied by the r59 SignedReview recorded under revision 58.

Revision 54 introduced the source/static gates for runtime-changing revisions 020 r18, 050 r14, 060 r13, and 070 r9. It replaces capacity-limit evidence with the exact two-field passive-delay contract, proves manual inline work has no pace/rate/cap admission, proves atomic passive attempt-plus-deadline persistence with no post-reservation shortening, and forbids every retired capacity key/control/admission path. It also proves blocklist database v2 migration and exact incoming-ID set-difference metadata, first-install/304/migrated sentinel semantics, identifier-free refresh/index phases, matching Activity/Settings status, and bounded lifecycle polling with checked enqueue rejection.

Revision 54 additionally binds the exact Threads 444 inline/report path: no composition-time process-global current-viewer gate in the adapter or factory; captured `Media.A75()` code first with `Media.A7o()` permalink only as strict fallback; stable canonical validation for both; exact `(no text in this post)` fallback after caption cleaning; exactly one post/reply control after Share; and only the bounded identifier-free `hook`, `request-unavailable`, `render`, and `adapter-exception` inline diagnostic stages. The earlier r53 SignedReview-i and Release-b prove only their frozen historical bytes. The first r54 SignedReview is failure-only evidence recorded under revision 55. At that checkpoint no passing current-series SignedReview, promotion, Release, or publication existed; the r59 SignedReview, the 2026-09-05 promotion, and the 2026-09-05 Release recorded under revision 58 later supplied those results, while no installation, live-row rendering, scrolling, account mutation, or network result is claimed by this document.

Revision 53 changes release evidence only; canonical runtime Java, rendered Smali, host rewrites, and APK behavior are unchanged. `SignedReview-h` passed the 325-case raw signed-primary-DEX bridge matrix and recovered all 38 targeted classes, then failed closed because `Test-PatchedApk.ps1` still required the nonexistent spelling `isCurrentPassiveMatch(` even though canonical Java, the exact resolution, recovered JADX, and raw DEX all use `currentPassiveMatch(`. The resolution schema now owns the exact ordered eleven-string `AutoBlockSync` wrapper baseline. `Test-ReleaseContract.ps1` AST-proves the signed-APK wrapper's literal array is identical and rejects stale, missing, duplicate, and nonliteral variants before another signed replay. `SignedReview-i` then passed the complete frozen review under resolution SHA-256 `1f98ff27f27456fe915eda71e1519c2a27f8ec4a2890051f0ae771db7125ce57` and produced exact work-local candidate SHA-256 `abf03f6d37cc7a5ea7dd3406c8d427b985914cec6d3390a3ca21f950fd20800b`. Explicit human approval promoted the Threads 444 resolution to `verified-current`, resolution SHA-256 `df092cc0d63e5ca6fb91d24ad9878a520360f1872ed3477182e173e3b3e92221`, with `updateSignedDexReviewRequired=false`. Fresh default Release-b then passed and transactionally published final APK SHA-256 `85124df5d5995af7ca3efd80b1a16f14b0e8fd380fc61e1bfe303b4037708ec4`; installation and runtime validation remain separate.

`SignedReview-h` is failure-only evidence. Its `work/patchlet-444-signed-review-20260904-h/build-report.json`, SHA-256 `0e12ad3b901b60138be0b7f21ba1cd7131d0eb0481b0ab1765664a4056a658ea`, records `failedStage: release-gates`, `artifactProduced: false`, verified publication rollback, null output binding, and runtime `not-run`. Its frozen 325-case bridge report passed and has SHA-256 `3c5c67893aae7a5db9d76a5a562f55ee9d698d6e60e9f4331e635ef85a972746`; that result proves the r52 raw-Dex generation-flow gate on h's frozen inputs, but the failed run did not authorize publication or replace the complete r53 review later supplied by SignedReview-i.

The historical r53 bridge matrix retained r52's 325 fixtures: 279 legacy plus 46 then-current cases, with one positive and 45 negatives in that lane. Its ten r52 invalid-generation additions are classified as four consumer call/accessor cases, four producer-caller register/exception-route cases, and two shared-callee forwarding/clamping cases.

Revision 52 corrects and strengthens release evidence only; it does not change canonical runtime Java, host rewrites, or APK behavior. SignedReview-f reached a signed work-only candidate and exposed a brittle readable-evidence expectation: JADX 1.5.6 preserves all three invalid-result generation latches but rewrites their local names differently in `loadTarget`, `PassiveLookupWorker.run`, and `currentPassiveMatch`. Both exact resolutions now bind the three recovered expressions individually at count one, and the catalog/release contract reject an alternate, missing, reordered, or count-drifted three-expression tuple. Targeted JADX remains secondary readable evidence: its local names and its rendered catch generation of `0L` are not semantic authority.

The authoritative raw signed-primary-DEX bridge inspector introduced by revision 52 proves the actual `IdMatch` result and observed-generation value flow from its producers through shared callees to all three invalid consumers. `loadTarget` and `currentPassiveMatch` must call the latch directly without an intervening generation clobber; `PassiveLookupWorker` must forward the same result generation through one structurally resolved, two-instruction, parameter-preserving synthetic accessor. `BlocklistStore.lookupId` and `isCurrentIdMatch` must preserve their observed wide generation registers through the reviewed normal, typed-handler, and catch-all routes rather than synthesize zero. Shared `IdMatch.access$200` must forward its `String` and wide arguments unchanged, and `IdMatch.invalid(String,J)` must apply `max(0, observedGeneration)` before storing that value in the constructed result's generation field. That then-current generated-D8 lane added ten compiling negatives: four consumer call/accessor clobbers, four producer register-clobber/zero-route mutations, and two shared-callee forwarding/clamping mutations. It grew from 36 to 46 cases, so the r52 matrix was 325 cases: the unchanged 279-case legacy regression lane plus the 46-case then-current lane, one positive and 45 negatives. Structured offsets identify all applicable proof paths; the legacy carrier reports inapplicable producer/consumer paths explicitly while retaining its applicable `currentPassiveMatch` proof.

SignedReview-f's failure report `work/patchlet-444-signed-review-20260904-f/build-report.json`, SHA-256 `e0c59de39d180deb61b4d49424527661747f8961e0f33bf7d7f44ca5b256631c`, records `artifactProduced: false`, failure at `release-gates`, verified publication rollback, and no runtime validation. Its 135,133,736-byte work-only APK has SHA-256 `c9ec5928912d896a7b52147d5be9a4409f143946bcb6218416d524742c79b870`; it is failure evidence, not a release artifact. The pre-r52 bridge report from that frozen run passed its then-current 315 cases (its recorded digest is malformed at 65 hex characters and is not reproduced here); that historical result does not satisfy the 325-case gate. The retained signed APK may be replayed only as bounded exact raw-Dex failure-byte evidence for the new consumer, producer-handler, and shared-callee checks. It could not validate the revised canonical tools or fixture matrix; SignedReview-i later supplied the required complete review on independent current bytes.

Revision 51 corrects release evidence only; it does not change the visibility callback or APK behavior. SignedReview-e reached the exact signed candidate and failed because the 444 targeted-JADX metadata still expected one `AutoBlockSync.unregisterVisibleControl(this)` occurrence. The exact resolution-selected 444 template and recovered signed DEX contain three intentional callsites: one guarded lifecycle `release()` callsite plus unconditional off-main revocation in the geometry and remember entries. Patchlet 060 r12 requires those direct off-main revocations so stale visibility authority is removed even when local registration state was cleared. The catalog now binds all targeted-JADX visibility API counts to the selected template and exact reviewed version tuple: register/update/unregister is `2 / 1 / 3` for 444 and remains `2 / 1 / 1` for historical 415. The release contract independently rejects a count-drift fixture before a signed replay. SignedReview-e remains failure-only evidence and does not authorize publication or runtime claims.

SignedReview-e's failure report `work/patchlet-444-signed-review-20260903-e/build-report.json`, SHA-256 `b6814ed2a821bfb1e75d5ea8d3519605fe6bf9215fc01f7e61c479b04e0e5ef6`, records `artifactProduced: false`, release-gate failure, and verified publication rollback. Its 135,133,736-byte signed work-only candidate has SHA-256 `dabb138503beaeb83cab778c29b107ffb6371d51f9e82c653b5a54c3de5592f2`; it is failure evidence, not a release artifact. The focused r51 resolution, catalog, and release-contract reports `work/r51-resolution-e-a.json`, `work/r51-catalog-444-a.json`, and `work/r51-release-contract-a.json` pass with SHA-256 `18ff2221618657e3387270287d56a5d4065fce0f0789c17aa22d6c460eba341d`, `1b2b16fc0e05cf1cc707dc14be5bf7b18c140617c4d0d42f8f234b6289d0b92b`, and `c30db0d9d52a0cc04a1428b4b520e72475ff372fc8c4dc265bd76f7ec98ec497`. The release-contract report records `2 / 1 / 3` and `countDriftRejected: true`; a fresh complete SignedReview was still required at that checkpoint and was later supplied by SignedReview-i.

Revision 50 repairs the release-contract fixture without changing APK behavior. Null fixture content now remains null instead of becoming an accidental empty Smali file. The `missing-candidate` negative proves `smali/X/02ja.1.smali` is absent before evaluation and requires the core evaluator's exact missing-file rejection. The matrix remains two positives, 11 negatives, and ordinary-path compatibility. A controlled 415 probe showed the two missing split-tool pins alone cannot make its current catalog pass without adding a missing `delivery` authority section, so that historical runtime boundary was not broadened.

Final focused r50 evidence passes on both retained decoded-tree arrangements and the complete release-contract harness. The c and d resolution reports `work/r50-resolution-c-final-b.json` and `work/r50-resolution-d-final-b.json` have SHA-256 `083f39b3da68c45409059d8974a90e547b6eb2f81fb8c62898e9ec0c1c4cee1f` and `941acf3951c895e9c12fdae55a21736ddfd7310dbdb701e694d7914c1ea0b86c`. The rebound catalog report `work/r50-catalog-444-final-b.json` has SHA-256 `a84af1aaeffa3720e27bdfa52e3694b0e4874244772b832d07b2c1a57cdee0cd`. The complete release-contract report `work/r50-release-contract-final-b.json` has SHA-256 `6a013fe93b5f3b5e3ca80a04d8daf93e4a4776fd901dfa82dc5170f0d75773bd` and records release-gate revision 50, two positives, 11 negatives, exact absent-path and failure-message evidence, and ordinary-path compatibility. These focused/static results are not a fresh SignedReview or runtime proof.

Revision 49 makes the pristine Smali proof for the Threads 444 null-seed `UserCache.A05` call independent of Apktool's nondeterministic Windows case-collision suffix assignment. The exact resolution names only `smali/X/02ja.smali` and `smali/X/02ja.1.smali`; the proof evaluator requires exactly one candidate to declare `LX/02ja;`, exactly one to declare the unrelated `LX/02jA;`, and counts the reviewed callsite only in the descriptor-selected target. Both observed suffix arrangements are positive fixtures. Missing files, duplicate target or sibling descriptors, extra or duplicate candidates, malformed or duplicate class declarations, unexpected descriptors, a callsite present only in the sibling, and mixed ordinary/collision path modes are negative fixtures. Patchlet 090 uniquely owns this proof ID and hash-pins the core evaluator, both schemas, catalog gate, and release-contract harness through each exact resolution. The historical 415 proof retains its ordinary exact-path behavior.

Final focused r49 reports pass both retained decoded-tree arrangements, the exact 444 catalog, and the complete release-contract harness. The release-contract report `work/r49-release-contract-final.json`, SHA-256 `0735adbcfe276da017949b861b25f917581f0444655ac980f50a649cb1aaeb7d`, includes two collision positives, 11 collision bypass negatives, ordinary-path compatibility, and the retained 315 bridge, 20 permalink, 13 proxy-bootstrap, and 96 updater lanes. These focused results do not replace a fresh complete `SignedReview`.

The earlier 444 SignedReview-b replay stopped in `signed-build` while invoking the Java signing command, which returned exit code 2. Its failure report `work/patchlet-444-signed-review-20260903-b/build-report.json`, SHA-256 `7dbde98d05e48ad499cf07b8b779f3017a28aaa3916b36c05620eb161d9b1fbe`, records `artifactProduced: false` and verified publication rollback. It retained only unsigned/aligned work products; no signed candidate, release check, publication, or installable artifact resulted. The report intentionally does not preserve credentials or a more detailed signing diagnostic.

The focused r48 bridge harness report `work/r48-bridge-suite-a-report.json`, SHA-256 `07086ef71b3707038281016b790f875c567799ac06b3c46f6a3b03204d2ca9e2`, passes all 315 generated-D8 fixtures. The r48 release-contract preflight report `work/r48-release-contract-a-report.json`, SHA-256 `debe38dc412fb3381d3fc71328ed7f7ae26c49787f3bf64b4ea84e2860216b86`, passes with 56 evidence expectations and 27 evidence-negative cases. The exact 444 catalog and resolution reports also pass. These are static/focused results, not a fresh complete `SignedReview`, publication, installation, account mutation, or device-runtime result.

Revision 48 closes a signed-wrapper evidence drift exposed by the retained 444 SignedReview-c candidate. Both reviewed bridge paths perform model preparation and the cache lookup/factory/placeholder chain: the ordinary Block path and the callback-free passive preflight. The signed wrapper, generated current carrier, and legacy regression carrier now each require raw `prepareModel = 2`, raw/definition/automatic-caller `passivePreflight = 1`, zero passive-preflight calls from the manual/bridge/other caller groups, bridge-owned `prepareModel = 2`, and cache lookup/factory/placeholder provenance `2 / 2 / 2`; both passive-preflight fail-closed booleans are mandatory. The three cache counts are exact resolution fields, and the AST evidence contract binds every duplicated count and boolean across all three lanes with missing, duplicate, nonliteral, divergence, and ancestry negatives. Revision 48 changes release evidence only; it does not relax the inspector or alter the generated/signed DEX implementation.

Revision 47 corrected the signed bridge-flow proof for its then-current one-target passive scheduler without allowing the historical batch-shaped carrier to authorize production. The resolution-pinned combined bridge suite was exactly 315 cases: 279 legacy regression cases plus 36 then-current `single-target` cases (one positive and 35 negatives). That lane proved the complete authority quartet immediately before target-budget admission, passive-running persistence, attempt reservation, and bridge dispatch; shared-lock ordering; provisional target-budget cleanup on every pre-reservation no-dispatch exit; absence of owner-held or local `List` state and backward same-owner selection edges; and rejection of started, failure, success, watchdog, and fresh-drain bypasses. The legacy positive was fixed at automatic `reviewedTerminalRoutes` / `terminalCatchRoutes` / `pacedNextPosts` = 5 / 1 / 1, while that production lane required 3 / 1 / 0 and `ownerMode: single-target`. Its release-tool AST contract covered exactly six callers, 22 contracts, and 26 invocations. These numbers are historical r47 requirements; they do not authorize r56.

The focused generated-Smali combined harness report `work/r47-authority-suite-final315-a-report.json`, SHA-256 `3781b4e9259906eaff511ddcb706a826156670e0823fa139207e79a140fbfde7`, records `status: passed` for all 315 fixtures. That focused result is not a complete `SignedReview`, signed-production-DEX review, release check, publication, or device-runtime result.

The canonical r47 release-contract preflight report `work/r47-release-contract-canonical-final-b.json`, SHA-256 `1a684966329e68d23390479ab872f68e620f8a5d890919e47341fa1c4b31c45b`, also passes. It binds the six-caller/22-contract/26-invocation graph, 30 current/legacy evidence expectations, 27 ordered bridge-inspector semantic bindings across four non-crossing lanes, and all 315 fixtures. This remains preflight evidence; it is not a complete signed review, promotion, publication, installation, or runtime result.

The first 444 `SignedReview` replay, `work/patchlet-444-signed-review-20260903-a`, proved a content-identical complete-series reapplication over 124,320 files, 8,163 directories, and 132,483 entries with tree SHA-256 `ea55b8175e731b9a2ee2bf3e8d66d7959fbf10994eac96d28db9e3f5721294c4`, then stopped at `release-gates` with `automatic_is_current_owner_shape`. Its build report records `artifactProduced: false`; there is no `release-check.json`, publication move, or `dist` output. The retained work-only APK is 135,133,736 bytes with SHA-256 `3c1e0c86a8c76f682f1aaf232ff7422c560a64d34899c250cf3836199f6d28d3`, and its primary `classes.dex` is 11,623,852 bytes with SHA-256 `9ccf7441e567405f947037267e5fc1c4e779e2e2c3f369a9d3edd9ccfe677caa`. That failure exposed a stale verifier assumption about the retired mutable batch owner; r47 source addressed the verifier shape, but those failed bytes did not validate the correction. At that checkpoint the 444 resolution remained `review-required`; SignedReview-i and the later promotion are separate evidence, and the old work-only bytes remain non-release artifacts.

Revision 46 adds a governed two-phase path for an exact resolution whose updater raw-Dex review blocker is still active. `SignedReview` accepts only `status: review-required` with `updateSignedDexReviewRequired: true`, requires signing credentials and an explicit device serial, confines its candidate to a strict child of workspace `work`, runs the complete `Test-PatchedApk.ps1` raw signed-Dex evidence and the isolated exact-primary-Dex Activity UI probe, and emits `validationMode: SignedReview`, `reviewOnly: true`, and `releaseEligible: false`. Supplying any publication path is rejected before a run directory is created, and a second guard prevents entry into the publication transaction. The default `Release` mode remains unchanged in authority: it accepts only `verified-current` with the blocker false, does not accept a review device parameter, reruns the full gates from a fresh input snapshot, and is the only mode that may publish. The pipeline, builder, signed-APK verifier, Activity probe, release contract, and AST contract module are exact-resolution hash-owned; an executable negative fixture proves that the review lane cannot create a run or `dist` output when publication is requested. The passive readable-evidence wrapper also derives the no-argument attached and empty-rectangle predicate tokens from the exact resolution, rejects malformed or non-boolean references, and therefore keeps both historical 415 and current 444 host symbols fail-closed without embedding one version's obfuscated names in shared release tools. The duplicate-root-DEX negative fixture now takes its seed bytes only from the canonical Smali carrier after verifying `assets.smaliCarrierSha256`; it no longer assumes that a split-derived universal APK has been copied to a root-level filename before the pipeline creates its run-local universal input. The updater invocation AST proof follows the fixture function's typed `ExpectedClassCount` parameter into the exact inspector slot while the caller still binds that parameter to the reviewed resolution value. Proxy-bootstrap source proof parses the resolution's exact DEX field reference, requires its owner/name/type tuple exactly once inside the matching reviewed DEX branch, and requires canonical reconstruction of the complete reference instead of searching for a combined literal that the structured inspector does not contain. The failure-report fixture accepts an explicit exact split-set path, chooses `SignedReview` or `Release` from the resolution's governed state, and deliberately mismatches only the Build Tools version after split snapshot binding; it then requires source-set, derived-source, and resolution hashes in the failure report. The independent SignedReview-with-publication negative still fails before creating its run directory.

Revision 45 is the exact-version restart for the Threads `444.0.0.45.85` base + arm64-v8a + xhdpi split set. It adds resolution-selected bridge, inline, reporting, and rendered-reference roots; two independently anchored runtime-selectable drawer layouts; counted provider-authority expectations; the reviewed 13-argument native mutation layout; and the direct captured-`Media` permalink/caption layout. The proxy bootstrap raw-Dex inspector retains its 11-argument wrapper contract while selecting only one of two code-pinned entry layouts: historical `classes10.dex` / `LX/319;->A05:LX/319;` or current `classes6.dex` / `LX/0143;->A06:LX/0143;`. The 444 layout additionally proves its original zero literal and argument-validation call before the parameter moves, followed by the exact superclass call, immediately adjacent bootstrap, and untouched next field read. A version, DEX, prefix, register, field, drawer, provider count, or template-root mismatch fails closed; historical 415 evidence cannot authorize the 444 release.

Revision 44 adds patchlet 005's source boundary ahead of every existing gate. A release now binds the exact three-member split APK set, its aggregate hash, two independent byte-identical APKEditor outputs, the derived standalone input hash, and the final signed output hash. The pinned merge must preserve all reviewed root DEX, arm64 native-library, and xhdpi resource payload bytes, remove split-delivery metadata, and remain unsigned until the normal isolated build/signing stage. These checks do not weaken or replace final native preservation, 16 KiB ZIP alignment, signature, signer, DEX, updater, or runtime boundaries.

Revision 43 corrects two release-proof boundaries exposed by the first signed-candidate replay. `android.permission.REQUEST_INSTALL_PACKAGES` is manifest metadata, not a DEX literal: the resolution and every final-DEX marker gate require it to be absent from `requiredHookCalls`. The signed APK gate still requires exactly one direct manifest `uses-permission` node through a structural `aapt2 dump xmltree` assertion; decoded-resolution proof parses the direct manifest child and exact Android namespace. Focused negatives reject missing, duplicate, wrong-element, unnamespaced, nested-child, and wrong-parent decoys. The updater graph digest now canonicalizes reviewed DEX representation differences: `const-string`/jumbo and `goto` widths, branch/switch/try/catch targets as semantic instruction indices, and only an unreferenced odd-offset payload-alignment NOP after an unconditional terminal. Generated and signed-candidate updater graphs both bind to `107247bf54dabb7c83cdf52c85cf8ef0b65b70f2ca8b4cd00289de04ee05c9e4`; an additional 25-class try/switch/branch carrier remains digest-identical after 248 forced string-jumbo rewrites and reassembly. The ordinary 95 semantic bypass negatives still reject by exact semantic code before hashing. No candidate or `dist` APK is manually patched by this revision.

Revision 42 retains the complete r41 updater/source/host contract and adds authoritative raw primary-DEX CFG, reaching-definition, exceptional-flow, and lifecycle proof. The production-equivalent Java 8/D8 carrier passed all 14 semantic checks with 24 scoped classes and semantic SHA-256 `d89b9170a5188691504087255b6d0b4b6f9a93d930516cec214e3fdb132799bc`; 95 exact bypass mutations were rejected by their reviewed semantic codes, with no digest-only fallback. The 12-argument inspector and 96-case harness expectations are AST-bound across the harness and signed-APK wrapper. `Test-PatchedApk.ps1` must run the same inspector against the exact final signed candidate before it can write a successful release report.

Security review approved the generated-D8 evidence and the exact resolution at that checkpoint became `verified-current` with `updateSignedDexReviewRequired=false`. That promotion was patchlet/source evidence, not a signed release claim: the r43 correction had no successful pristine replay, published update metadata, APK download, installer handoff, startup, or device-runtime success. SignedReview-i later satisfied the then-current pre-publication review portion, and Release-b then produced the fresh exact-source signed candidate whose primary `classes.dex` passed the identical updater inspector, whose manifest contained the exact install permission once, and which was transactionally published. Every passed authoritative updater release subresult is AST-bound to that raw primary-DEX result; targeted JADX remains complementary readable evidence and cannot independently grant updater authority. A dedicated negative contract mutation removes the raw-DEX binding from a JADX-bearing subresult and must fail closed. No update metadata, APK download, installer handoff, startup, or device-runtime success is claimed. A started metadata read or explicitly tapped download may finish and cache after pause, but UI and installer launch remain current-foreground-Activity-bound; network failure must preserve only a previously verified required policy and must not manufacture a first-run lock.

Revision 40 makes no production-tree or decoded-rewrite edit of its own. It retains revision 39 and adds blocking canonical, generated-Smali, exact-host, targeted-readable, raw signed-DEX, and positive/negative fixture proof for patchlet 020 r16. Every invalid store result must bind the generation actually observed. Unknown-generation and current-generation faults must latch the passive pause, while suppression is legal only for a known observed generation strictly older than the latest committed verified generation; a 304, equal/older replacement, or unproven generation cannot clear or bypass the latch. The typed durable refresh deadline read must be exception-safe as well as exact-`Long` typed, with any exception, wrong type, overflow, or implausible future value pausing ordinary refresh fail closed.

Revision 40 also requires the complete signed payload to fail closed on any non-object target, malformed or noncanonical platform field, or malformed Threads row, preserving the prior verified generation. Only a structurally valid canonical non-Threads row may be excluded. Fixtures and signed evidence must prove both sides so a parser cannot broaden valid exclusion into malformed-row tolerance. Static proof remains bounded to the exact candidate; at the r40 checkpoint the resolution remained `review-required` with no fresh pristine replay, signed candidate, or runtime proof.

Revision 39 makes no production-tree or decoded-rewrite edit of its own. It adds the blocking release contract for patchlet 020 r15 and patchlet 030 r6. Canonical Java, rendered Smali, exact-host evidence, positive and negative fixtures, targeted readable recovery, and raw signed DEX must prove that one scheduler owner carries exactly one immutable numeric target and cannot retain a list, cursor, loop, `next()` continuation, or other same-owner path that selects a second target. Completion releases that owner with the current pace deadline; only a fresh drain that checks the manual queue first may select another current passive match.

The r39 passive contract also requires five fail-closed boundaries. Every refresh attempt must persist the exact 600,000 ms ordinary `list_refresh_not_before` deadline before work starts; worker-start rejection must re-advance it from rejection time before releasing or waking the lane. Pause, resume, and process recreation must honor the remaining delay, while only the bounded viewer-scoped forced path may bypass it without clearing or transferring it. Indexed lookup must preserve three outcomes—valid match, valid non-match, and invalid/unavailable/corrupt—and invalid store state must latch passive work paused until a strictly newer committed verified generation clears it; an older in-flight invalid result cannot re-latch after that commit. Signed import and stored invariants must reject duplicate/conflicting normalized usernames, final admission must reject disagreement between the stored and visible normalized username, and verified timestamp text must parse to its identical epoch-millisecond value before replacement and again on stored metadata reads. Authority grants—registration, visible=true, manual enqueue, owner acquisition, and `BlockRun.begin`—must reject off-main-thread entry; off-main pause/update/unregister may only revoke and must serialize through `PASSIVE_ADMISSION_LOCK`.

Under the shared generation/admission exclusion boundary, the callback-free exact-SHA `ThreadsBlockBridge.passivePreflight` must run after current exact-ID membership is established but before `passive_running` persistence or atomic attempt-plus-delay reservation. `already_blocked_success` creates no attempt or pacing deadline and submits no callback or native mutation; a fixed preflight failure likewise reserves nothing. A mutation-ready result is not continuing authority: complete foreground, Activity/viewer, manual priority, passive-delay, completion/quarantine state, visibility, generation, exact-ID/username membership, and store-validity authority must be checked again immediately before running persistence, reservation, and ordinary bridge dispatch. Every pre-reservation exit leaves no deadline; once reservation succeeds, no path may clear, shorten, or roll back it. Manual work persists its attempt but bypasses passive pacing, and no retired target/run/hour/day capacity path may be restored. The ordinary bridge must then repeat exact model-ID and native already-blocked preparation before mutation. Static evidence remains bounded proof of the exact candidate; at the r40 checkpoint the resolution remained `review-required` with no fresh pristine replay, signed candidate, or runtime proof. SignedReview-i later supplied static review evidence, but live scrolling, database behavior, and account mutation remain unproved.

Revision 38 makes no production-tree or decoded-rewrite edit of its own. It adds the blocking release contract for patchlets 020 r14, 050 r11, and 060 r10's indexed passive blocking. Canonical, rendered, host, fixture, and signed-Dex evidence must prove one atomically replaced indexed signed-list generation; exact checked 600,000 ms foreground refresh cadence with next-resume catch-up rather than an Android background-timing claim; metadata-only 304 handling; previous-generation preservation on every failure; and deletion-only ownership of the retired SharedPreferences list blob. It must prove exactly one lifecycle-bound clipped-viewport observer on the existing post/reply UFI control, memory-only Compose routing, background indexed numeric-ID/username lookup with unavailable distinct from non-match, and no database/network/scheduler/report/bridge work from the observer. It must also prove no fetch-to-queue edge or database/list enumeration into mutation work, durable still-visible match admission, manual priority, complete pre-reservation viewer/foreground/visibility/generation/membership/quarantine/limits checks, one-target single-flight dispatch through `ThreadsBlockBridge`, interruption/uncertainty quarantine without automatic retry, and zero passive report creation. Current visibility coverage is post/reply action rows, including those rendered in profile post lists; profile headers, search results, and follower/following lists are explicitly outside the resolution. A fresh pristine r38 replay, hash-bound contract review, human review, promotion, and live scrolling/runtime validation remain required. The published r37 APK and its UI probe contain no r38 passive-blocking evidence.

Revision 37 makes no production-tree or decoded-rewrite edit. It retains revision 36's bootstrap AST/count/binding/order contract and adds blocking canonical-Java, targeted-JADX, and static host proof for patchlet 080 r3. Release evidence must distinguish newly established guard `paused_guard_active` and retained guard `paused_guard_retained` as separate **Paused**, traffic-blocked topologies; `unprotected_prior_routing` as retained forwarding with the warning that earlier numeric DIRECT exclusions may remain; and `paused_vpn` exclusively as no TUN, **Unprotected**, inactive, with direct traffic possible. Enabled configuration with runtime `disabled` must also map to explicit **Unprotected**/inactive/direct-possible status instead of stale **Starting** after revoke, service destruction, or notification disconnect. The gate must also prove Proxy Settings' lifecycle-bounded 500 ms poll: start on resume, stop on pause, full status refresh only when the credential-free runtime token changes, checked initial and recurring `Handler.postDelayed`, and a visible **Unavailable** state on rejection. A fresh pristine r37 replay, hash-bound contract review, human review, promotion, and isolated Activity probe remain required; these static contracts are not device, account, runtime, SOCKS reachability, VPN routing, or network-packet proof.

Revision 36 made no production-tree or decoded-rewrite edit. It hardened only the release contract around the proxy bootstrap proof. PowerShell AST must bind the signed wrapper and positive fixture harness to the resolution's exact numeric literals for 13 bootstrap fixtures and 11 bootstrap-inspector arguments, and both invocations must supply the identical ordered ten semantic bindings after the APK argument. Blocking proxy negative-contract fixtures must reject duplicate, missing, nonliteral, divergent, or reordered expectations and bindings. The targeted-JADX metadata must be the ordered exact 28-class inventory including `ModStateStore`; the Application target must contain exactly `super.attachBaseContext(` followed by `ProxyBootstrap.install(` in `orderedStrings`; and the raw signed-Dex bootstrap gate must occur exactly once before targeted JADX. No fresh pristine r36 replay was completed, so these requirements did not validate an APK or retroactively validate the failed `-c` bytes.

Revision 35 made no production-tree or decoded-rewrite edit. The fresh revision-34 replay at `work/patchlet-socks5-proxy-20260902-c` rebuilt, aligned, and signed the exact patched tree after complete-tree idempotency, then failed closed before publication because JADX 1.5.6 skipped all 555 instruction units in `BarcelonaAppShell.attachBaseContext`. The signed method still contains the exact post-super `ProxyBootstrap.install(Context)` hook, but skipped output cannot prove it. Revision 35 therefore enabled JADX's reviewed inconsistent-code recovery for secondary readable evidence and added a separate hash-pinned raw signed-Dex flow inspector with positive and negative fixtures. The raw gate owns the exact entry parameter moves, superclass call, immediately adjacent bootstrap call, same context register, original next field read, sole bootstrap caller, branch/handler/try exclusion, and exact fallback-reference absence. No JADX rendering is authoritative for that hook, and r35 did not retroactively validate or publish the failed `-c` bytes.

Revision 34 makes no production-tree or decoded-rewrite edit. The first SOCKS5 replay, `work/patchlet-socks5-proxy-20260902-b`, reached a signed run-local candidate only after complete-tree idempotency passed, then failed closed before publication because the broad DEX scanner treated `socksProxyPort` as a case-insensitive substring of pristine Threads method `getSocksProxyPort()`. Its build report records `failedStage: release-gates`, `artifactProduced: false`, a null output binding, no publication move, verified cleanup, and runtime `not-run`; those bytes are failure evidence only. The exact source-SHA resolution now proves `.method public final declared-synchronized getSocksProxyPort()I` in `smali_classes11/com/facebook/proxyservice/observer/ProxyServiceBroadcaster.smali`. Only the ambiguous lowercase broad substring is excluded. Canonical Java and every signed targeted proxy class—including the exact Application hook—must instead forbid the exact-case fallback set `allowBypass(`, `System.setProperty(`, `ProxySelector.setDefault(`, `socksProxyHost`, `socksProxyPort`, and `java.net.useSystemProxies`; every other global DEX prohibition remains unchanged.

Revision 31 makes no production-tree or decoded-rewrite edits of its own. It preserves the blocking `report-post-permalink-contract` while matching its proof to the reviewed final-D8 control flow. `ReportRequest.isValidForNewQueue()` may use one shared return only when set-valued reaching definitions prove exactly one literal-true assignment dominated by the base-validity and nonempty-permalink guards, every other reaching assignment is literal false, the two reviewed guards are the only conditional branches, and no exception range exists. `ReportPayload.toJson()` must bind its self-validity result to the one branch whose valid side reaches the writes and whose invalid fallthrough reaches only one pre-write return of the initial empty root; exactly one populated return of that root must be dominated by both `targetUrl` and evidence-container writes, while the sole handler reaches one distinct empty catch root. Twenty positive/negative fixture cases now exercise the shared join, extra-success rejection, validity bypass, populated-root substitution, and retained clobber, slot, storage, receiver, queue, payload-value, array and handler failures. The inspector interface remains exactly 21 arguments, targeted JADX remains 19 classes, and the independent bridge inspector remains 28 arguments/275 fixtures. The failed r30 run below remains verifier-shape failure evidence; the subsequent r31 replay is the successful release evidence for this revision.

Revision 29 first added the mandatory post-permalink request/payload contract and the eighteenth `ReportPayload` targeted-JADX class. Revision 30 added the signed-Dex value-flow and strict-sanitizer evidence split. Revision 31 retains that policy and soundly recognizes the exact shared-return and early-root D8 shapes above.

The frozen r30 replay `work/patchlet-post-permalink-r30-20260902-a` reached a signed run-local candidate and then stopped at `release-gates`: the old origin lattice merged the reviewed literal-one and literal-zero assignments at `ReportRequest.isValidForNewQueue()`'s shared return and emitted `request_validity_true_return_count`. Its build report records `status: failed`, `artifactProduced: false`, a null output binding, no publication move, verified rollback, and runtime `not-run`. Direct r31 inspection of those failure-evidence bytes additionally proves the populated-payload root after accounting for the guarded pre-write empty-root return and distinct catch root. Those bytes remain unpublished failure evidence and cannot count as an r31 release.

The fresh r31 replay `work/patchlet-post-permalink-r31-20260902-b` passed its build, signed release, and complete-tree idempotency reports and transactionally published `dist/ThreadsMod-CloneBlocker-415.0.0.26.77-arm64-v8a-post-permalink.apk`. The published APK is 81,305,196 bytes, SHA-256 `47f8f70fdf615463de336ddb34edd34c2f81f4e4014b2fa4f8d85f19b5dc5cd3`, package `com.threadsmod.barcelona`, version `415.0.0.26.77` (`versionCode 508504469`, minimum/target SDK 28/36), 16 KiB aligned, and has one v2 Android Debug signer certificate SHA-256 `317e3f3813f3b1ec324717faf1bb78f954f49a12122181a85b42338ca10dd079`. Reapplication was content-identical across 115,685 files, 7,821 directories, and 123,506 entries at SHA-256 `46b2c64c61d976783d8759d90412dce8cf8c93200ae73bc68356a98f8fad669c`. The release passed all 275 bridge and 20 permalink fixtures plus 19 targeted-JADX recoveries. Runtime is `not-run` because no ADB device was available; installation, account, or network success is not claimed.

Revision 28 makes no production-tree or decoded-rewrite edits of its own. It removes the ambiguous broad forbidden substring `Send report`: the exact pristine source already contains `Send reports blocking` at `decompiled/apktool-3.0.3/smali/X/0Dw.smali:1238`, so the case-insensitive substring scanner cannot use that label as absence evidence. One-control topology, zero legacy Report renderer/tag, and retired UI-class proofs remain authoritative. It retains revision 27's inspector-arity fix and revision 26's signed-Dex and release-tool contracts and fixes the stale outer inspector-arity guard by binding one exact Java `args.length` literal to `/release/expectedDexBridgeFlowInspectorArgumentCount = 28`; four negative fixtures reject stale, nonliteral, missing, or duplicate guards before signed fixture evidence can count. It also retains revision 25's blocking combined-modal/report contracts and strengthens the signed-primary-DEX private-seam proof: every `Lthreadsmod/` and `Lcom/threadsmod/` class is scanned, private media lookup, author lookup, and username calls must be zero, and the only mod-namespace author-ID and already-blocked calls remain the reviewed `ThreadsBlockBridge.prepareModel` calls. Three new negative fixtures exercise those forbidden stable-code calls; the resolution pins the complete 275-case fixture count and the five reviewed terminal routes. The exact-SHA host rewrite remains the sole owner that resolves media, displayed author, numeric ID, username, author model, and media model into one immutable row snapshot. Revision 28 retains the SHA-pinned `ThreadsMod.ReleaseToolContract.psm1`: it AST-proves the complete reviewed five-caller, thirteen-contract, fourteen-invocation script graph, including the pipeline-to-signed-wrapper edge, uses only exact ordered parameters declared by each callee, rejects unowned script calls, and binds exactly one wrapper/harness literal apiece for five terminal routes, 275 expected fixtures, and 275 observed fixtures. Eight invocation and six evidence negative AST fixtures make missing, duplicate, positional, splatted, direct, undeclared, nonliteral, resolution-drifted, or divergent forms fail closed. The historical `work/patchlet-cache-id-diagnostics-20260901-b` run used revision-21 inputs: it completed exact-source patching, complete-tree idempotency, rebuild, alignment and signing, then failed closed because `Test-PatchedApk.ps1` passed undeclared `AndroidSdk` to `Test-DexBridgeFlowInspector.ps1`. Its build report records `artifactProduced: false`; it wrote no `release-check.json`, made no publication move, and is not a release artifact. Revision 22 removed that argument. Revision 23 first replaced the weak literal-absence assertion with a narrow caller/callee AST proof; revision 28 retains, pins, and extends that proof before any signed fixture evidence can count.

The failed r23 run `work/patchlet-cache-id-diagnostics-20260901-c` matched the exact source SHA-256, proved a content-identical 123,532-entry reapplication, rebuilt, aligned, and v2-signed a run-local candidate. The frozen `DexBridgeFlowInspector` itself passed that signed candidate, including sole-mod-caller and reviewed private-seam evidence, and its 272-fixture contract report passed. The enclosing `Test-PatchedApk.ps1` evidence check nevertheless retained `reviewedTerminalRoutes = 4` while the canonical fixture and real result now contain five reviewed terminal routes, so release gates failed before `release-check.json` or publication; `dist` remained absent for this run. Revision 24 aligns that expectation with the canonical five-route evidence and AST-binds the field/value relationship so future fixture growth cannot leave a stale outer count. This patchlet remains the final read-only contract that decides whether a candidate may become an installable artifact.

The independent revision-24 replay `work/patchlet-cache-id-diagnostics-20260901-d` passed its build, release, complete-tree idempotency, and 272-fixture reports and transactionally published `dist/ThreadsMod-CloneBlocker-415.0.0.26.77-arm64-v8a-cache-id-diagnostics.apk`. The published APK is 81,309,292 bytes, SHA-256 `042c780ff15f9f2a5a94261eb7a6f0a989d7c3f91f19fa98b71a21c3d7d372fb`, package `com.threadsmod.barcelona`, version `415.0.0.26.77` (`versionCode 508504469`, minimum/target SDK 28/36), 16 KiB aligned, and has one v2 signer certificate SHA-256 `317e3f3813f3b1ec324717faf1bb78f954f49a12122181a85b42338ca10dd079`. Reapplication was content-identical across 115,711 files, 7,821 directories, and 123,532 entries at SHA-256 `d6a7414bf1ae7c5949a15a641afe68ffe8df97fe305060d4842e6e2cb42c4343`. Runtime is `not-run` because no ADB device was available; installation, account, or network success is not claimed, and the `-c` bytes remain failed r23 evidence.

## Required order

`report-post-permalink-contract` is an ordered, blocking gate within the report-factory/payload checks below: it must pass after exact resolution/template application and before any signed candidate can satisfy the report delivery or publication gates.

1. Before importing the helper module, read-lock and hash the executing pipeline and module, and require the parsed pipeline AST to equal the locked on-disk entry text. Hash and copy the exact source APK plus the complete resolution directory into a fresh run snapshot; require the snapshotted resolution to match the selected mode (`review-required`/blocker true only for `SignedReview`, `verified-current`/blocker false only for `Release`) and remain bound to that source. Hash the complete canonical `patchlets/**` tree and every external build/validation tool input, prove it contains those initially attested entry bytes, then hold read locks on every frozen file and persistent change monitors on every frozen tree through review completion or publication. PowerShell necessarily parses the entry script before its first statement, so this is an ordinary/concurrent post-start drift control rather than a malicious-host root of trust.
2. Validate catalog, patchlets, resolution and rewrite sets against their schemas. Hash-check and import `ThreadsMod.ReleaseToolContract.psm1`, parse the exact resolution-owned release graph, and fail unless every call passes each exact ordered argument declared in the callee's top-level `ParamBlock`. Reject every splat, duplicate, missing argument, positional extra, undeclared name, direct/non-ampersand call, or unowned script call. The r53 325-case matrix remains historical evidence for its frozen bytes; it cannot authorize r56 or the current r58 series. The current signed wrapper, positive harnesses, and resolution must AST-bind identical literal fixture counts, inspector arities, ordered semantic members, and current owner mode. Current negatives must cover the shared lock and complete authority immediately before passive-running persistence, atomic passive delay/attempt reservation, and dispatch; no retired target/run/hour/day admission or cleanup; no owner/local target collection or backward selection edge; no post-reservation deadline clearing/shortening; and the retained observed-generation producer/consumer/shared-callee flows. Report proof must bind captured-media `A75()` code first and `A7o()` permalink fallback, exact no-text fallback, absence of composition-time viewer gates, and strict stable canonicalization. Run all release-tool negative fixtures before signed evidence can count. Require the collision fixture `Send reports blocking` to match `Send report` under the same case-insensitive substring semantics, exclude only that ambiguous broad string from `forbiddenDexStrings`, and retain exact one-control/zero-legacy-render/tag/class proofs. Separately bind the pristine `getSocksProxyPort()` method as the exact-SHA collision for broad `socksProxyPort`, exclude only that ambiguous broad substring, and require exact-case fallback-string absence in canonical Java plus every signed targeted proxy class. Record deterministic results in catalog/release reports; text search alone is insufficient.
3. Verify every rewrite postcondition and the complete apply ledger.
4. Remove only Apktool's exact derived `decoded/build` cache, capture the complete canonical decoded file/directory inventory before reapplication, and acquire a zero-exception read lock before idempotency. Every canonical decoded-lock assertion must omit tree and file mutation allowances, and that immutable lock stays held through publication. After the content-identical no-op proof, copy the locked canonical tree into a fresh, disjoint run-owned build-working directory with real file copies, hidden files and empty directories preserved and reparse points rejected. Require the copy's SHA-256, file count, directory count and entry count to equal both the canonical freeze and idempotency-v2 proof.
5. On the build-working copy, read-lock every existing file except the exact root `AndroidManifest.xml`; permit only the initially absent exact `build/**` subtree and exact root `AndroidManifest.xml` / `AndroidManifest.xml.orig` operations used by pinned Apktool. Every mutable working-lock assertion must repeat those exact tree/file lists. Structural monitoring excludes recursive `build/**` churn, ignores directory-only `Changed` noise, and fails closed on any unexpected create/delete/rename/file change or monitor error/overflow. Bind `Build-PatchedApk.ps1` to the complete working-copy digest/counts and build only from that copy. Acquire the final candidate lock immediately after build; then remove only the exact working `build` cache, require `.orig` absent and the complete working inventory restored, overlap the mutable monitor with a new zero-exception working-tree lock, and retain the sealed working tree, canonical decoded tree and candidate locks through every gate and publication/failure cleanup. The deterministic contract test executes this exact lock/copy/manifest lifecycle, proves the stable working-file handle count excludes only the one existing manifest, blocks stable writes, rejects an outside rename and injected monitor error, and structurally inspects every real pipeline lock assertion.
6. Test archive integrity, reject every duplicate or non-canonical ZIP `FullName` across the complete source and candidate archives, preserve every `lib/**` entry, and hash-compare every `assets/**` file except exact resolution-reviewed generated-Dex exclusions. Each exclusion must exist on both sides and pass the replacement DEX integrity gate; directory, glob, traversal, and unexplained exclusions are forbidden.
7. Verify every DEX header, require unique DEX entry names and the exact root set `classes.dex` through the resolution-owned terminal index, and enforce the primary method-reference budget. Search required endpoint/class/hook evidence and forbidden DEX strings only inside entries that first pass DEX magic, checksum and signature validation; a non-DEX APK entry can neither satisfy nor trip those gates. Broad forbidden matching is case-insensitive and therefore cannot distinguish lowercase `socksProxyPort` from the pristine `getSocksProxyPort` identifier; the exact-SHA proof and per-class signed targeted-JADX fallback negatives are mandatory substitutes for only that one literal. Compile the SHA-pinned direct-string inspector against dexlib2 inside the SHA-pinned Apktool JAR and require the exact `ReportClient.post` owner method to contain exactly one `HttpsURLConnection.setRequestMethod(String)` invoke whose exact String argument register is written by the immediately preceding `const-string "POST"`/jumbo instruction, with no branch, switch, or exception-handler entry directly into the invoke. Positive, decoy-GET, different-method, duplicate-call, and branch-merge fixtures are blocking contract tests.
8. Enforce read attempts against exactly GitHub raw `published/blocklist/v3/manifest.json`, jsDelivr `@published/blocklist/v3/manifest.json`, then the reviewed AWS `blocklist/v3/manifest.json`, with objects fetched only as pattern-validated `objects/<64 lowercase hex>.json` or `.ndjson.gz` names under the same three bases, consulted in that order with only the strictly newest verified candidate installed; enforce the exact AWS `/v1/reports` URL (`ReportEndpoint.writeUrl()`) as the reviewed report write destination and the exact AWS `/v1/installs` URL (`requiredStatisticsEndpoint`, `ReportEndpoint.statsUrl()`) as the second reviewed POST destination, each through its own accessor with no fallback and with no other write host or path; prove the instruction-bound POST method, redirect refusal, and complete `tree55.com` absence.
9. Verify clone identity separation and component preservation.
10. Verify both mod activities occur once, are non-exported, and have no intent filter. Prove from canonical Java that Activity attaches a visible shell before its state read, catches corrupt-state rendering into a review card, gives stat-card layout ownership to the two non-null parent `addView` calls, and has no null layout call. Recover both activities from the signed primary DEX, require the reviewed headings and shell-before-snapshot order, and reject `setLayoutParams(null)`. Activity and Settings must expose identical dedicated list-fetch, total-record, new-record-or-Unavailable, and database-state/generation status from patchlet 020's immutable snapshot. Their bounded main-handler polling must start only while resumed, remove callbacks on pause/destruction, check every `postDelayed` result, and replace prior live state with visible **Unavailable** when enqueue is rejected. Verify only the live-session hook can publish the process-memory viewer, the retired raw-viewer preference is deletion-only, and a process-restored Activity has no report scope. The exact-primary-DEX emulator probe must parse at most 1 MiB of DTD-free UI XML with external resolution disabled; derive a nonzero root viewport; require every matched node to have nonzero bounds intersecting that viewport; match each fixed label as exact visible node text; and require `List fetch:`, `Records:`, `New this refresh:`, `Database index:`, and `Inline control:` as one contiguous ordered multiline node with a nonempty value after every prefix. Its executable matcher contract is exactly one positive and nine bypass negatives: content-description-only, mid-line prefix, wrong prefix order, missing prefix, malformed XML, zero bounds, offscreen bounds, empty status value, and split status nodes.
11. Verify the drawer click/item/row classes, native keyed Settings row immediately before the bottom spacer, and exact resolution-owned renderer/modifier bindings.
12. Verify inline classes, immutable row binding, Share-adjacent state capture, exactly one Block renderer after both Share container closes, zero Report renderer/tag/icon, and exactly one UFI helper call across both template families. Require one exact-SHA helper appended to the resolved host class to read the bound media key, resolve media and displayed author, read author ID and username once, and create one `InlineBlockRequest.createHostBound` snapshot containing opaque author/media models. That exact request must feed the sole adapter and Report factory. Stable inline/Report code must have no raw media lookup, author lookup, author-ID, username, or already-blocked seam, while `ThreadsBlockBridge.prepareModel` remains the sole reviewed mod-namespace author-ID caller. The adapter and factory must contain no process-global current-viewer read or self-target gate before rendering; controller and queue boundaries must retain those checks. For Threads 444, require one captured-media `Media.A75()` code call before a strict `Media.A7o()` fallback, both routed through stable canonical validation, and exact `(no text in this post)` fallback after caption cleaning. Require exactly the four bounded identifier-free inline diagnostic stages `hook`, `request-unavailable`, `render`, and `adapter-exception`, with no identifier, username, URL, object, exception text, or stack trace. Require exactly one request/model-keyed visibility callback on that same control modifier, attached plus non-empty clipped-root-bounds truth, deduplicated false/true transitions, all remember/forget/abandon cleanup, and only one call to patchlet 020's memory-only registration API. The exact 444 helper's modifier-default bit and base-modifier substitution branch must be source-proven, while template, generated carrier, and signed DEX prove the decorated modifier reaches the button with that bit clear; the former defaulting mask is a blocking negative. Reject any database, fetch, scheduler, report, or bridge call from the Compose callback and any claimed profile-header/search/follower hook.
13. Verify one compact `Block and report` modal contains bounded identity, immutable excerpt, reviewed reason, persisted `Also block this profile`, one dynamic `Block`/`Report` positive action, concise disclosure, and Cancel. Its positive action must always enter one-step durable reporting and may additionally route Block only through the scheduler. Preserve the sole entry after blocked/success states and forbid a dedicated Report action, second editor/review, note, consent checkbox, one-click bypass, outbox controls, and removed section copy.
14. Execute the passive minimum 2–60 seconds, passive maximum 3–60 seconds, defaults 4–10, maximum-not-below-minimum rule, every exact whole-second accepted step, and fractional/range/order negatives independently. Execute the production store against host-only Android preference fakes to verify one-commit atomic success; failed-commit rollback of Integer, String, Boolean, Long, Float, StringSet, and missing-key snapshots; partial/wrong-type/invalid rejection; read-exception closure; and an uncertainty latch that survives failed rollback until a later successful complete save. Prove exactly two Settings controls and live keys; retired manual-delay, passive/hour, target-budget, total/hour, total/day, and attempts/run keys are deletion-only and have no UI or admission use. From uncommented executable bodies and signed DEX, prove manual inline work bypasses passive pacing and every capacity check, while every attempt is persisted before bridge dispatch. Passive preflight must precede running state and atomic attempt-plus-selected-deadline reservation; all complete authority checks repeat immediately before running persistence, reservation, and dispatch under the admission lock. Pre-reservation exits create no deadline, and no post-reservation path may clear, shorten, or roll back it. Every passive-delay/backoff wait and scheduler-critical Handler enqueue must check acceptance; only best-effort toast delivery may omit it. Manual priority, null passive model hints, cache-first direct-ID fallback, exact model-ID validation, and absence of the retired profile-fetch/`lookup_failure` path remain blocking proofs.
15. Verify native success plus local completion-save failure installs a bounded process-local viewer/target latch before attempting the strict `completion_review_threads_<viewerId>` set, rejects the target during selection and again before dispatch, creates no backoff/automatic queue/automatic retry, limits the set to 200, durably pauses automatic work for corrupt/oversized/full state, and retains a local pause on exception or commit failure. Apply the same no-retry quarantine to an elapsed or rejected post-dispatch watchdog: automatic work has no backoff, while inline work becomes abandoned and cannot run again until explicit review. Require identifier-free `CB-LOC-301` and `CB-MUT-205` with only reviewed `review-saved` / `review-local-failclosed` state. Verify explicit Retry/Retry all atomically transition only selected retryable manual items and remove their matching quarantine before clearing the local latch.
16. Verify Report consumes the same patchlet-060 host-bound request, never re-resolves media, author, author ID, username, or already-blocked state, and uses only its captured opaque media model for the exact-SHA caption/code/permalink chains. Require `Media.A75()` code-first and `Media.A7o()` strict fallback provenance, stable canonical URL validation, exact `(no text in this post)` fallback only after cleaned caption emptiness, mandatory non-empty username/canonical ID/Unicode-safe 280-unit excerpt/reason, initiating Activity/viewer/foreground-generation binding at the explicit queue action, concise modal and complete Settings privacy/retention/cancel-race disclosure, stable pseudonym, and absence of raw viewer/session/cookie/token data.
17. Verify one distinct durable pre-network outbox row per accepted modal action, attempt reservation, one full fail-closed validation per foreground generation, cross-table report-ID disjointness, stored self-target rejection, indexed per-delivery mutations, insertion-only history trimming, plausible timestamps and overflow-safe wakes, storage review state, exact SQLite v2 schema plus lossless fail-closed v1 migration, 1,000 pending/500 history/16 KiB UTF-8 payload bounds, 15-attempt terminal policy, exact HTTPS endpoint, redirect refusal, success only on 2xx plus complete valid-UTF-8 strict duplicate-free JSON with one boolean `ok: true`, and independent optional Block scheduling.
18. Compile every SHA-pinned raw inspector against dexlib2 and invoke each signed-Dex gate in its resolution-owned order before targeted JADX. PowerShell AST must prove wrapper and positive-harness fixture counts, inspector arities, and ordered semantic arguments are literal, identical, and resolution-bound; blocking negatives reject missing, duplicate, nonliteral, divergent, reordered, substituted, or extra forms. The proxy bootstrap lane retains its exact 13-fixture, 11-argument, ten-member proof. The targeted-recovery helper must load the signed candidate once and recover the resolution's ordered exact class inventory with no missing, duplicate, extra, skipped, or reordered target; readable JADX remains secondary to raw signed-Dex CFG/value flow.

    The current passive lane must prove observed-generation preservation through lookup producers, accessors, and all consumers; strict database-v3 schema (h32 bucket column and index, UNIQUE username index, chunk/group/staging tables) with the chained v1-to-v2-to-v3 migration, root-signature and object-SHA-256 verification before any parse, whole-chunk rejection and per-bucket replacement; exact incoming-ID set difference, first-install count, 304 zero, and migrated `-1` semantics; retained-generation behavior during indexing/failure; and identifier-free refresh/index state. It must prove only the two passive delay fields, manual no-pace/no-cap behavior, absence of retired capacity admission, preflight before running/reservation, complete locked authority immediately before running persistence, atomic passive delay/attempt reservation, and dispatch, plus conservative non-clearing deadlines after reservation. One immutable target, no visible collection/batch/same-owner continuation, manual priority, checked wakes, watchdog quarantine, and no fetch-to-mutation/report edge remain mandatory.

    The current inline/report lane must prove exactly one host snapshot and one post-Share renderer, no composition-time viewer gate, the four fixed identifier-free hook diagnostic stages, captured `Media.A75()` code first with `Media.A7o()` strict fallback, exact empty-caption fallback, sixth constructor-slot and final-field flow, all request/controller/client guards, canonical `targetUrl` and sole `evidence` equality, and absence of private identity re-resolution or a second Report UI. The bridge lane must preserve cache-first direct-ID fallback, model-ID equality, narrow fixed-stage handlers, callback dispatch ownership, and completion-review quarantine. No raw inspector may be replaced by source/template/JADX appearance, and no historical r53 fixture count or tuple may authorize the current evidence tree; the r59 SignedReview and the first 2026-09-05 Release ran the then-current r58 tree; the r61 SignedReview and the 2026-09-06 Release ran the current r60 tree.

The same signed-Dex result must prove that passive and manual scheduler ownership handoff uses the literal true state before each bridge catch, a false handoff result returns before the catch, no alternate edge enters each sole `Throwable` range, and each outside handler routes fixed `bridge_exception` to the exact failure owner. The six `BridgeCallback` implementations must consume `Delivery.run` directly without an immediate relay and retain reviewed started/status, failure, completion-save, quarantine-before-release, success, and normal/exceptional ownership-release effects that are CFG-reachable and post-dominate the applicable path. All-path checks include every covering handler and forbid normal entry to terminal catch tails. Manual nested and outer release proofs reject infinite cycles that avoid release. Each passive owner retains exactly one immutable target and releases into a fresh manual-first drain that honors the already-persisted passive deadline; manual work may run immediately and must not extend that deadline. `BlockRun` has no target collection, cursor, target-selection loop, `next()` method, or same-owner target-selection Runnable. Every scheduler-critical enqueue uses its live Handler/Runnable/delay, checks the returned Boolean, and reaches the reviewed refusal outcome. Post-dispatch watchdog expiry or refusal reaches quarantine, not ordinary backoff.

The signed-Dex scheduler release-core proof is exact as well. It binds the scheduler lock and both catchall monitor-exit/rethrow paths; owner-token equality and nonzero guards; every zero/empty/false state clear; and the complete post-release viewer/session/activity decision tree. The current-context path derives manual-pending from the reviewed `nextQueued` result and schedules it without passive delay; otherwise it requires literal `resumeAuto` plus `isEnabled` and honors the committed passive deadline. A viewer or Activity replacement may schedule only the zero-delay current-context drain, which must still select manual work first and re-read passive pacing before passive admission. Decimal-ID, length, null, equality, branch-polarity, argument, or delay-route drift fails closed.

The bridge-owned entry topology is exact as well: callback-free `passivePreflight` begins at its session cast, reaches lookup/factory/placeholder/shared preparation and every fixed result, and reaches no callback or native mutation; direct-ID `block` begins at the session cast and reaches lookup/factory/placeholder/dispatch; `blockResolved` begins at the null/resolved presence branch and reaches both reviewed paths; `prepareModel` begins at author-ID and reaches equality, predicate, and outcomes; `blockModel` begins at preparation and reaches its result and mutation routes. A method-entry return followed by otherwise convincing unreachable seams must fail. Signed-Dex and fixture flow must also prove that the ordinary bridge repeats exact-ID and already-blocked preparation after passive preflight and before native mutation.

Revision 38 adds a separate passive-flow proof. Canonical and generated code, exact host templates, host fixtures, and signed DEX must agree that the checked foreground refresh delay is exactly 600,000 ms, including a full-interval failure/overdue rearm; an idle forced refresh starts directly and only one arriving behind active work becomes a bounded viewer-scoped intent; verified 200 responses replace the indexed generation atomically; 304 changes only fetch metadata; every fetch/schema/transaction/invariant failure retains the previous generation; and the legacy list preference is never read. The proof must reject any path from fetch completion or database enumeration to passive queue insertion. It must bind the single post/reply clipped-viewport callback to the immutable request, require an idempotent registration heartbeat on every geometry callback, keep that callback memory-only, show background indexed numeric-ID and normalized-username lookup with conflict/unavailable fail-closed results, and trace only a still-visible numeric-ID match into durable passive state. It must then prove raw-type-checked bounded completed-ID state, manual-queue priority, final viewer/foreground/visibility/generation/membership/quarantine/limits recheck, generation replacement serialized against that recheck through committed passive-running state, reservation, and shared-bridge dispatch, one selected target per ownership, no automatic retry after interruption or uncertain mutation, and no Report constructor/outbox/delivery call from passive flow. Positive and negative fixtures must cover the exact cadence, failure-loop rejection, forced-intent scope, background overclaim, atomic replacement/rollback, 304 behavior, retired blob, database unavailable, username conflict, corrupt/oversized completion state, fetch-to-queue and batch-enumeration rejection, observer duplication/heartbeat/lifecycle cleanup, invisible/stale matches, manual priority, final recheck, scheduler-only bridge routing, and passive-report absence.

Revision 54 supersedes the capacity-specific portions of that historical r38 proof. The current lane has only the passive minimum/maximum pair, checks authority at running/reservation/dispatch, atomically persists attempt plus selected deadline before dispatch, never rolls that deadline back, and forbids target/run/hour/day admission. Its database proof is schema v2 with exact added-ID metadata and the Activity/Settings status snapshot/polling contract described above.
19. Verify 16 KiB zip alignment.
20. Verify the APK signature and required signer certificate.
21. Verify package, launcher, SDK and ABI metadata with `aapt2`.
22. Record the final artifact digest and bind the snapshotted source APK, exact resolution file, canonical decoded-tree/idempotency proof, restored sealed build-working tree, continuously locked candidate, and output APK SHA-256 values in both release and build reports. Immediately before the success report and publication move, re-hash the canonical source, stage-consumed snapshot copies, resolution directory, complete `patchlets/**` tree, resolution-pinned assets, external tool inputs, canonical decoded input, sealed build-working input, and candidate APK. After the move, read-lock and re-hash the actual `dist` file, recheck every frozen input, and commit the success report inside the rollback transaction. Any build failure records `derivedWorkCopy` evidence before releasing locks: canonical decoded freeze/lock state, working-copy inventory, exact manifest and `.orig` presence/readable hashes, exact build-cache residue/inventory, and bounded monitor evidence. Any later outer failure removes and verifies absence of that run-owned exact target; cleanup refusal records truthful residue/hash evidence.

Every required gate is blocking. A warning may describe a runtime boundary, but it cannot substitute for an error gate. Stage tools consume locked copied source/resolution semantics while existing canonical patchlet/tool files and the canonical decoded tree remain read-locked; only the derived build-working copy receives the exact monitored Apktool mutation contract. Persistent monitors retain unexpected add/remove/rename/file-write and error/overflow evidence, so a transient edit cannot influence a later stage and then disappear before the final hash check. An ordinary failure produces a drift/build report with `artifactProduced: false`, immutable initial source/resolution bindings, separately recorded observed-at-failure hashes, truthful derived-work residue when the copy exists, a null output binding, and no published path. Publication uses a hash-checked temporary file under `dist` followed by a same-directory move only after all gates, the success report, and the pre-publish freeze pass; post-move drift or report failure removes only that run's exact temporary/committed target and verifies absence. If the filesystem refuses exact cleanup, the failure is never mislabeled non-artifact: the report records `artifactProduced: true`, the residue path/hash when readable, and rollback failure evidence.

For the current exact APK, Apktool `--all-src` reassembles `assets/longtail/classes.dex`, so byte preservation is intentionally narrowed only for that exact file. `DexInspector` still validates its DEX magic, header, checksum and signature and records it in the all-DEX inventory. All other 1,180 `assets/**` files are byte-hash compared to the pristine source.

## AI boundary

AI may summarize failures, group likely drift causes and propose bounded follow-up tasks. It may not mark a gate passed, change expected counts, add an endpoint exception, approve a signer mismatch, suppress a preservation difference, or publish an APK. Only deterministic tool output populates gate results.

## Runtime boundary

The isolated exact-primary-DEX probe manifest now exposes exactly three test-only
activities and no permissions or services. It launches Activity, Settings, and
SOCKS5 Proxy Settings, requires their reviewed visible headings, records
`networkOrAccountActionAttempted=false`, and removes only the probe package.

These blocking checks establish static patch, archive, and signature integrity only after a fresh run passes them. `SignedReview` invokes the separately SHA-pinned Activity probe against that same candidate, but even a successful no-permission probe would not prove drawer navigation, the inline button in a live Threads row, composition timing, `A75()`/`A7o()` live values, modal rendering, refresh/status transitions, exact new-record counts, real database migration/fault behavior, passive pacing across process recreation, login, Block animation/removal, account mutation, report acceptance, or any other live network behavior. Current visibility coverage is only post/reply action rows, including profile post-list rows, not profile headers, search results, or follower/following lists. The r53 probe and published Release-b bytes predate r56, while r55 failed only at the stale Activity matcher; none validates the current evidence tree. The focused corrected r56 probe is tool evidence only. The r59 SignedReview (candidate SHA-256 280fa71d00315dbadcece0de5b3fcf19dffd542ee95a8052f979c2dbe86cf9b8), the 2026-09-05 10:48 promotion, and the first 2026-09-05 Release supplied that chain for the r58 tree55 series; the r60 SignedReview, the 2026-09-05 17:05 promotion, and the second 2026-09-05 Release supplied it for the superseded r59 threads55 series; and the r61 SignedReview (candidate SHA-256 7252940817521e93455b040d355bbd8e35951cb917a6b36bb16bf5d9f45fe23a), the 2026-09-06 promotion to resolution SHA-256 04e92a63eedd051b6c471607d929ab9737290b7664b0a13dbfa06d8653bfc9e5, and the 2026-09-06 Release (SHA-256 abcfb52fc23b1d819488789566c62189b5297183bf5e39652f17f132272d3bf8) supply it for the current r60 series. Installation and explicit runtime tests remain outstanding: the published APK has not been installed on any device, and no runtime fact exists for its bytes.
